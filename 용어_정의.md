# 용어 정의 — 우리가 감지하는 것은 "낙상"이 아니다

> 작성일: 2026-08-21 (코드 리네임 적용 완료)
> 관련: [설계값_근거.md](설계값_근거.md) §3, [코드_리뷰.md](코드_리뷰.md)

---

## 1. 문제 — "낙상"은 우리 시스템보다 넓은 말이다

WHO의 낙상(fall) 정의는 이렇습니다.

> 부주의하게 **지면·바닥 또는 더 낮은 곳에 멈추게 되는 사건**

이 정의에는 **침대에서 떨어짐, 의자에서 미끄러짐, 계단에서 구름**이 전부 포함됩니다.
그런데 우리 센서는 **보행기 프레임**에 달려 있습니다. 침대에서 떨어지는 사람 옆에서
보행기는 가만히 서 있습니다. **감지할 수 없고, 감지하려고 만든 것도 아닙니다.**

용어가 틀리면 다음 세 가지가 연쇄적으로 틀어집니다.

| | 결과 |
| :--- | :--- |
| **문헌 적용** | 낙상 감지 문헌은 전부 **몸통 착용** 센서 기준이다. 그 임계값을 그대로 쓰면 안 된다 |
| **성능 주장** | "낙상 탐지율 95%"라고 쓰면 침대 낙상도 잡는다는 뜻이 된다. 과대 주장이다 |
| **한계 설명** | "사용자만 넘어지고 보행기는 서 있는 경우"가 **결함**처럼 보인다. 실은 **범위 밖**이다 |

세 번째가 특히 중요합니다. 용어를 바로잡으면 **결함이 명시된 범위 경계로 바뀝니다.**

---

## 2. 표준 분류에 이미 정확한 말이 있다

### 2.1 산업재해 발생형태 분류 (고용노동부 / KOSHA)

| 분류 | 정의 | 우리 사건과의 관계 |
| :--- | :--- | :--- |
| **떨어짐(추락)** | 높은 장소에서 떨어지는 것 | ✗ 해당 없음 |
| **넘어짐(전도)** | **사람이 거의 평면 또는 경사면, 층계 등에서 구르거나 넘어진 경우** | ✅ **사용자에게 일어나는 일** |
| **부딪힘(충돌)** | 움직임·동작으로 기인물에 접촉·충돌 | ✅ **원인 중 하나** |
| **깔림·뒤집힘** | **세워져 있는 물체 등이 쓰러진 경우, 건설기계 등이 운행 중 뒤집혀진 경우** | ✅ **보행기에 일어나는 일** |

추락과 전도가 헷갈릴 때의 구분 기준도 명시되어 있습니다:
**1 m 이하 높이는 전도, 그 이상은 추락.**
보행기 사고는 명백히 **전도**입니다.

→ **즉 우리 사건은 "보행기의 뒤집힘"과 "사용자의 넘어짐(전도)"이 동시에 일어나는 것**이고,
   센서는 **앞의 것만 직접 측정**합니다.

### 2.2 영문 문헌의 용어

영문에서는 **"walker-related falls"** / **"rollator falls"** / **"tip-over"** 가 확립된 용어입니다.
미국 응급실 자료 기준 **연간 약 47,312건**의 고령자 보행보조기구 관련 낙상 부상이 보고되며,
그중 **87.3%가 워커(walker)** 관련입니다. 지팡이보다 **7배** 많습니다.

→ 이 수치는 **기획 배경(계획서 §1)에 그대로 쓸 수 있는 근거**입니다.
   "보행기는 낙상을 막으려고 쓰는 기구인데, 정작 보행기 관련 낙상 부상이 지팡이의 7배"라는
   문장은 과제의 정당성을 강하게 뒷받침합니다.

---

## 3. 제안 — 3층 용어 체계

핵심은 **"측정하는 것"과 "알고 싶은 것"을 분리해서 정의**하는 것입니다.
공학 보고서에서 measurand(측정량)와 event of interest(관심 사건)를 구분하는 것은 기본 규범이며,
이걸 지키면 위 §1의 세 문제가 한 번에 해결됩니다.

| 층위 | 용어 | 영문 | 정의 |
| :--- | :--- | :--- | :--- |
| **① 측정량** | 프레임 충격 가속도 · 자세각 | frame impact acceleration, attitude | IMU가 실제로 재는 물리량 |
| **② 판정 사건** | **보행기 전도(轉倒)** | **walker tip-over** | 시스템이 실제로 판정하는 사건. 보행기가 넘어짐 |
| **③ 관심 사건** | **보행기 동반 낙상** | walker-related fall | 우리가 알고 싶은 사용자의 상태 |

**②와 ③의 관계: 전도는 낙상의 대리 지표(proxy)이며, 완전히 일치하지 않는다.**

```
        보행기 전도 (측정 가능)        보행 중 낙상 (관심 대상)
        ┌───────────────┐          ┌───────────────┐
        │               │          │               │
        │   ①  보행기만  │  ② 동반  │  ③ 사용자만    │
        │      넘어짐    │   전도   │     넘어짐     │
        │  (오탐 위험)   │ (정탐)   │  (미탐 — 범위 밖)│
        │               │          │               │
        └───────────────┘          └───────────────┘
              ↑                            ↑
     보행기를 눕혀둠            손잡이를 놓치며 넘어짐
     차에 실음                  (보행기는 서 있음)
```

**이 그림 한 장이 보고서에서 매우 강력합니다.** 시스템의 능력과 한계를 동시에,
정직하게, 한눈에 보여줍니다. 심사위원이 물어볼 질문을 먼저 답하는 셈입니다.

---

## 4. 구체적 권고 — 이 단어를 쓰십시오

### 4.1 주 용어: **보행기 전도(轉倒)** / walker tip-over

첫 등장 시 **"보행기 전도(轉倒, walker tip-over)"** 로 한자·영문을 병기하고,
이후에는 "전도"로 씁니다.

**왜 "전도"인가:**
- 고용노동부 재해발생형태 공식 용어 **"넘어짐(전도)"** 와 정합
- 영문 **tip-over** 와 1:1 대응
- "1 m 이하 = 전도" 라는 공식 구분 기준에 정확히 부합

**왜 "전복"이 아닌가:**
「전복(顚覆)」은 *완전히 뒤집힘*(capsize)의 뉘앙스가 강합니다.
보행기가 옆으로 쓰러진 경우, 앞으로 고꾸라진 경우까지 포괄하려면 **전도**가 정확합니다.
현 계획서 §1의 "보행 중 전복"은 → **"보행 중 전도"** 로 고치는 것을 권합니다.
(다만 완전히 뒤집힌 상태를 특정할 때는 "전복"을 그대로 써도 됩니다.)

**"전도"의 동음이의어 주의:** 전도(傳導, 열전도) / 전도(電導).
센서·전자 문서에서 혼동 우려가 있으므로 **첫 등장 한자 병기는 생략하지 마십시오.**
혼동이 심하다고 판단되면 순우리말 **"보행기 넘어짐"** 도 KOSHA 용어와 정합하는 안전한 대안입니다.

### 4.2 용어 치환표

| 지금 쓰는 말 | 바꿀 말 | 비고 |
| :--- | :--- | :--- |
| 낙상 감지 시스템 | **보행기 전도 감지 시스템** | 과제명·시스템명 |
| 낙상 감지 | **전도 감지** | 시스템이 하는 일 |
| 낙상 확정 / 사고 확정 | **전도 확정** 또는 **사고 확정** | 후자도 무방(중립적) |
| 낙상 임계값 | **전도 판정 임계값** | |
| 보행 중 전복 | **보행 중 전도** | 계획서 §1 |
| 낙상 탐지율 95% | **전도 탐지율 95%** | ★ 성능 주장은 반드시 교체 |
| — | **보행기 동반 낙상** | 관심 사건을 지칭할 때만 |

### 4.3 실험 시나리오 이름

CSV 태그가 곧 보고서의 실험 조건 이름이 됩니다. **적용 완료된 이름은 아래와 같습니다.**

| 이전 | 현재 | 구분 |
| :--- | :--- | :--- |
| `fall_forward` | **`tipover_front`** | 양성 (전방 전도) |
| `fall_side` | **`tipover_side`** | 양성 (측방 전도) |
| `fall_back` | **`tipover_rear`** | 양성 (후방 전도) |
| `fall_recover` | **`tipover_recover`** | 음성 — 해제되어야 함 |
| `put_down` | **`place_down`** | 음성 — 보행기를 눕혀둠 |
| `curb` / `sudden_stop` / `normal_walk` / `car_load` | 변경 없음 | 음성 |

`tools/replay.py` 는 파일명 접두사 `tipover_` 를 양성으로 분류하며,
`*_recover` 가 들어가면 음성으로 취급합니다.
용어 변경 이전에 수집한 `fall_*` 로그도 양성으로 인식하도록 **하위 호환**을 두었습니다.

---

## 5. 이 재정의가 만드는 부수 효과 (전부 좋은 쪽)

### 5.1 문헌 인용이 정직해진다

이제 이렇게 쓸 수 있습니다.

> 선행 낙상 감지 연구(Bourke, Kangas 등)는 모두 **신체 착용형 센서**를 전제로 하며,
> 관심 사건이 **사용자의 낙상**이다. 본 시스템은 **보행기 프레임 부착형**으로
> 관심 사건이 **보행기 전도**이므로, 선행 연구의 임계값은 **초기값으로만** 사용하고
> 실측을 통해 재확정하였다.

이건 변명이 아니라 **문제를 정확히 이해했다는 증거**입니다.

### 5.2 "보행기는 서 있는데 사용자만 넘어짐"이 결함에서 범위 경계로 바뀐다

**전:** "낙상 감지 시스템인데 낙상을 놓칩니다" → 결함
**후:** "본 시스템의 판정 사건은 보행기 전도이며, 사용자 단독 낙상은 검출 범위 밖이다.
       이를 보완하기 위해 SOS 물리 버튼을 두었고, 손잡이 그립 센서 추가를 향후 과제로 제시한다" → 설계

같은 사실인데 평가가 완전히 달라집니다.

### 5.3 성능 지표를 방어할 수 있게 된다

"전도 탐지율"은 우리가 실제로 측정한 것이고, "낙상 탐지율"은 아닙니다.
숫자가 같아도 **방어 가능한 주장인지 아닌지가 갈립니다.**

---

## 6. 코드 반영 — 적용 완료 (2026-08-21)

문서뿐 아니라 **코드 식별자까지 일괄 변경했습니다.** 테스트 31종은 변경 후에도 전부 통과합니다.

### 6.1 식별자

| 이전 | 현재 |
| :--- | :--- |
| `FALL_NORMAL` / `FALL_STABILIZING` / `FALL_CHECKING` / `FALL_ASKING` / `FALL_CONFIRMED` / `FALL_REPORTED` | `TIPOVER_*` |
| `FALL_DISPLAY` | `TIPOVER_DISPLAY` |
| `FALL_TILT_DEG` | `TIPOVER_TILT_DEG` |
| `FALL_IMPACT` / `FALL_ASK` / `FALL_CONFIRMED` (경보 등급) | `TIPOVER_IMPACT` / `TIPOVER_ASK` / `TIPOVER_CONFIRMED` |
| `PATTERN_FALL_ASK` | `PATTERN_TIPOVER_ASK` |
| `fall_state` (필드·변수) | `tipover_state` |
| `fall_level` (인자) | `tipover_level` |
| `fall_state_to_level()` | `tipover_state_to_level()` |
| `AccidentMonitor` | `TipoverMonitor` |
| `_show_fall_popup()` | `_show_tipover_popup()` |

### 6.2 파일·서비스

| 이전 | 현재 | 비고 |
| :--- | :--- | :--- |
| `accident_monitor.py` | **`tipover_monitor.py`** | 전도 판정 FSM |
| `fall_monitor.py` | **`safety_monitor.py`** | 전도 판정 외에 경고·버튼·RTK·배터리도 담당하므로 중립적인 이름으로 |
| `tests/test_fall_fsm.py` | **`tests/test_tipover_fsm.py`** | |
| `systemd/walker-fall.service` | **`systemd/walker-safety.service`** | |

`safety_monitor.py` 로 고른 이유: 이 프로세스는 전도 판정만 하는 것이 아니라
**안전 필수 경로 전체**(판정 + 경고 출력 + 물리 버튼 + RTK + 배터리 + 신고)를 담당합니다.
`tipover_monitor.py` 는 그 안의 판정 FSM 하나를 가리킵니다.

### 6.3 산문

코드 주석과 문서의 "낙상"을 "전도"로 바꿨습니다. 단 다음은 **의도적으로 남겼습니다**:

- `states.py` 최상단 용어집 — 낙상과 전도를 대비시키는 것이 목적
- `config.py` 의 선행 연구 인용 — Bourke·Kangas·Chen은 **실제 낙상** 데이터를 다룬 연구이므로
  그 맥락에서는 "낙상"이 정확합니다

### 6.4 남은 호환성 문제

- **CSV 열 이름이 `fall_state` → `tipover_state` 로 바뀌었습니다.**
  용어 변경 이전에 수집한 CSV에는 옛 열 이름이 남아 있으나,
  `tools/replay.py` 와 `tools/analyze_logs.py` 는 이 열을 읽지 않으므로 분석에는 영향이 없습니다.
- `legacy/` 폴더의 이전 버전 코드는 **변경하지 않았습니다.** 역사적 참고용입니다.

---

## 7. 계획서 수정안 (§1 기획 배경)

**현재:**
> 보행 중 전복, 턱 걸림 등으로 인한 **낙상 및 충격 사고** 발생 시 골든타임을 확보

**수정안:**
> 보행 중 충돌·턱 걸림 등으로 발생하는 **보행기 전도(轉倒, walker tip-over)** 를 감지하여
> 사고 발생 시 골든타임을 확보한다.
> 보행보조기구 관련 고령자 낙상 부상은 미국 응급실 기준 연간 약 47,000건으로 보고되며,
> 그중 87.3%가 워커 관련으로 지팡이의 약 7배에 달한다.
> 본 시스템의 판정 사건은 **보행기 전도**이며, 이는 보행 중 낙상의 대리 지표(proxy)이다.
> 사용자가 보행기를 놓친 채 단독으로 넘어지는 경우는 검출 범위 밖이며,
> 이를 보완하기 위해 수동 SOS 호출 버튼을 함께 제공한다.

---

## 참고문헌

1. 고용노동부 — 산업재해 발생형태 코드 (떨어짐/넘어짐/부딪힘/깔림·뒤집힘 정의 및 1 m 구분 기준).
   [공공데이터포털](https://www.data.go.kr/data/15049607/fileData.do?recommendDataYn=Y) · [KOSHA 재해사례](https://portal.kosha.or.kr/archive/disaster-case/accident-case)
2. Stevens, J.A. et al. — *Unintentional fall injuries associated with walkers and canes in older adults treated in U.S. emergency departments*, J Am Geriatr Soc (2009). **← 연간 47,312건, 워커 87.3%**
   [PubMed](https://pubmed.ncbi.nlm.nih.gov/19555423/)
3. *Gender-Specific Patterns of Injury in Older Adults After a Fall from a Four-Wheeled Walker (Rollator)*, Swiss Level 1 Trauma Centre (retrospective study).
   [PMC](https://www.ncbi.nlm.nih.gov/pmc/articles/PMC11855301/)
4. Bagalà, F. et al. — *Evaluation of Accelerometer-Based Fall Detection Algorithms on Real-World Falls*, PLOS ONE (2012). — 신체 착용형 전제.
   [PLOS ONE](https://journals.plos.org/plosone/article?id=10.1371%2Fjournal.pone.0037062)

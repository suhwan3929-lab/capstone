# Pi 5 / 개발 PC 공통 (Jetson은 ROS 2 환경이라 별도)
#
# ⚠ 개발 PC는 Python 3.14, Pi/Jetson 기본은 3.10~3.12다.
#   버전이 다르면 재현되지 않는 문제가 생긴다. 팀 전원이 같은 버전을 쓸 것.
#   권장: Python 3.11

# ── 필수 ────────────────────────────────────────────
pyserial>=3.5
requests>=2.31
numpy>=1.24

# ── 선택 (없어도 동작하며, 해당 기능만 비활성화된다) ──
tkintermapview>=1.29   # 대시보드 지도
gpiozero>=2.0          # Pi 부저/LED/진동/버튼  (Pi에서만)
lgpio>=0.2             # gpiozero 백엔드 (Pi 5 권장)
smbus2>=0.4            # 배터리 연료 게이지 I2C (Pi에서만)

# ── 분석 도구 (tools/) ──────────────────────────────
pandas>=2.0
matplotlib>=3.7

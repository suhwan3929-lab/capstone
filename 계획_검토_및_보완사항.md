# 계획 검토 보고서 — 고령자 보행기 안전 보조 시스템

> **대상 문서:** `capstone 요약 문서.md` (전체판: 1장 ~ 6.2장, 코드 포함)
> **검토 관점:** ① 타당성 ② 실현 가능성 ③ 보완 필요 항목 ④ 보안(Security)·안전(Safety)
> **작성일:** 2026-08-20

---

## 0. 요약

계획의 **골격은 타당하고, 설계 밀도도 캡스톤 수준을 넘어선다.** 특히 QoS 정책을 데이터 성격별로
나눈 점, 1학기는 AC 어댑터로 전원 변수를 차단하고 2학기에 배터리로 통합하는 단계 전략, NTRIP VRS를
미리 식별한 점은 실무적으로 정확한 판단이다.

문제는 **다른 층위**에 있다.

| 층위 | 상태 |
| :--- | :--- |
| 아키텍처·전략 | 양호 |
| **코드 구현** | **현재 코드는 그대로 돌리면 오작동한다.** 최소 4건의 확정적 버그 (§4) |
| **센서 설정** | **설정값 자체가 목적과 충돌한다.** 진동 제거 필터가 충격을 지우고, 가속도계 측정범위 미확인 (§3) |
| **누락 챕터** | 정량 요구사항, 검증 계획, 일정·역할분담, 예산, 기구/무게중심, 인터넷 회선 |

가장 중요한 한 줄: **§4.1의 `linear_acceleration` 단위 문제와 §4.2의 GPS (0,0) 문제를 잡지 않으면,
시연 당일 부저가 계속 울리거나 보호자에게 대서양 한가운데 좌표가 전송된다.**

---

## 1. 이전 검토 대비 변화

| 항목 | 상태 |
| :--- | :--- |
| 전원 전략 | ✅ **해소** — §6.1에 단계별 전략 명시 (다만 용량·무게 산정은 여전히 없음, §5) |
| RTK 보정정보 확보 | ✅ **해소** — §6.2 NTRIP VRS 연동 계획 명시 |
| 듀얼 LiDAR 캘리브레이션 | ✅ **해소** — §6.2에 R/T 매트릭스 과제로 명시 |
| 낙상 임계값 실험 데이터 | ✅ **해소** — §6.2에 데이터 로깅 계획 명시 |
| 3단계 경고 로직 | ✅ **해소** — §6.2에 명시 |
| **로버 인터넷 회선(LTE)** | ❌ **여전히 없음** — NTRIP·Webhook 둘 다 필수인데 BOM에 모뎀/SIM이 없다 |
| **취소 버튼 / SOS 버튼** | ❌ 여전히 없음 |
| **ROS 2 배포판 정합성** | ❌ 여전히 없음 (Pi 5 + Ubuntu 22.04 문제) |
| **카메라 없는 "AI 비전" / SLAM 용도** | ❌ 여전히 미해결 (내부 모순) |
| **정량 요구사항·검증·일정·역할분담·예산** | ❌ 여전히 없음 |
| **기구·무게중심** | ❌ 여전히 없음 |

---

## 2. 네트워크 및 ROS 2 설정 검토 (§3.1 ~ §3.3)

### 2.1 [치명] `eth0`이 Raspberry Pi 5에는 존재하지 않을 가능성이 매우 높다

```bash
export CYCLONEDDS_URI='<CycloneDDS><Domain><General><NetworkInterfaceAddress>eth0</NetworkInterfaceAddress></General></Domain></CycloneDDS>'
```

- **Raspberry Pi 5의 온보드 이더넷은 `end0`으로 잡힌다** (RP1 사우스브리지 드라이버 기준, Pi OS Bookworm / Ubuntu 계열 공통).
  Jetson은 `eth0`이다. **같은 `.bashrc`를 두 보드에 복사하면 Pi 쪽에서 DDS가 인터페이스를 못 찾는다.**
  → 반드시 각 보드에서 `ip -br link` 로 실제 이름을 확인하고 보드별로 다르게 설정할 것.
  → 더 안전한 방법은 **인터페이스 이름 대신 IP 주소를 쓰는 것**이다 (`192.168.10.2`).
    `NetworkInterfaceAddress`는 이름과 IP를 모두 받으므로 IP를 쓰면 이름 변경에 영향받지 않는다.
- `NetworkInterfaceAddress`는 최신 CycloneDDS에서 **deprecated**다. 신규 문법 병기 권장:

```xml
<CycloneDDS><Domain>
  <General>
    <Interfaces><NetworkInterface address="192.168.10.2"/></Interfaces>
    <AllowMulticast>true</AllowMulticast>
  </General>
  <Discovery>
    <ParticipantIndex>auto</ParticipantIndex>
    <Peers><Peer address="192.168.10.1"/></Peers>
  </Discovery>
  <Internal><MinimumSocketReceiveBufferSize>8MB</MinimumSocketReceiveBufferSize></Internal>
</Domain></CycloneDDS>
```

- **`.bashrc`에 XML을 인라인으로 넣지 말고 `CYCLONEDDS_URI=file:///etc/cyclonedds.xml` 형태를 권장**한다.
  인라인 XML은 따옴표 이스케이프 사고가 잦고, systemd 서비스로 자동 실행할 때 `.bashrc`는 읽히지 않는다.
  (**중요:** 최종 시연은 부팅 시 자동 실행이어야 한다. `.bashrc` 기반 환경변수는 systemd 환경에서 적용되지 않으므로,
  지금부터 `Environment=` 또는 `EnvironmentFile=` 로 옮겨둘 것.)

### 2.2 [중요] Point Cloud 유실 — 소켓 버퍼

대용량 `PointCloud2`를 UDP로 보내면 커널 수신 버퍼 부족으로 **조용히 프레임이 깨진다**(ROS 2에서 매우 흔함).

```bash
sudo sysctl -w net.core.rmem_max=8388608
sudo sysctl -w net.core.rmem_default=8388608
```

영구 적용은 `/etc/sysctl.d/`에 파일로. 위 XML의 `MinimumSocketReceiveBufferSize`와 **함께** 설정해야 효과가 있다.

### 2.3 QoS 정책 검토

- **점군 = BEST_EFFORT / depth 1** → 판단 정확. 최신 프레임 우선이 맞다.
- **알림 = RELIABLE / depth 10** → 판단 정확.
- **다만 IMU 구독을 `sensor_qos`(depth=1)로 둔 것은 재검토 필요.**
  100 Hz IMU에서 depth 1은 스케줄러 지터나 콜백 지연이 생기면 **충격 피크 샘플 자체를 버린다.**
  낙상 감지는 "최신 값"이 아니라 "피크를 놓치지 않는 것"이 목적이므로 **depth 10~50** 권장.
  (호환성은 문제 없다. 발행자 RELIABLE + 구독자 BEST_EFFORT 조합은 정상 매칭된다.)

### 2.4 누락된 네트워크 설계 2가지

1. **Jetson의 인터넷 경로가 없다.** Jetson은 유선으로 Pi에만 연결되고 Wi-Fi/LTE는 Pi에 있다.
   그러면 Jetson은 `apt`, `pip`, NTP 어디에도 못 나간다.
   → **Pi 5에서 IP forwarding + NAT(iptables MASQUERADE)** 를 켜서 Jetson의 게이트웨이를 192.168.10.2로 지정.
2. **시각 동기화가 없다.** 두 보드 타임스탬프가 어긋나면 "충격 시점의 위치·점군"이 어긋난다.
   → **Pi를 chrony NTP 서버로, Jetson을 클라이언트로.** 더 좋게는 **ZED-F9P의 UTC/PPS를 시각 소스로** 사용.
   → 관련: **Raspberry Pi 5에는 RTC 배터리 커넥터가 있다.** 배터리를 달지 않으면 인터넷 없는 실외에서
     콜드 부팅 시 시스템 시각이 엉뚱해지고, 코드의 `datetime.now()`가 **잘못된 사고 시각**을 전송한다. (§4.2-⑦)

### 2.5 좌표계(TF) 정의 부재

`base_link` 기준 `lidar_left`, `lidar_right`, `imu_link`, `imu2_link`, `gps_link`의 정적 변환이
문서에 없다. §6.2의 "R/T 매트릭스 정밀 측정"이 결국 이 TF 트리로 표현되어야 한다.
REP-103/105(x-forward, y-left, z-up) 준수하고 URDF 또는 `static_transform_publisher`로 명문화할 것.

---

## 3. 센서 설정 및 알고리즘 검토 (§4)

### 3.1 [치명] `<avca_e1>` — 진동 제거 필터가 감지하려는 충격을 지운다

문서는 "능동형 진동 제거(AVCA) 활성화 — 노면 진동 필터링"으로 적고 있다. 그런데 이 시스템의 목적은
**노면에서 올라오는 짧고 큰 가속도 이벤트를 잡아내는 것**이다. **노이즈와 충격은 시간 스케일이 겹친다.**
진동을 지우는 필터는 충격 피크도 함께 깎아낸다.

→ **권고: 이것이 IMU를 2개 쓰는 이유가 되어야 한다.**

| IMU | 설정 | 용도 |
| :--- | :--- | :--- |
| IMU #1 (프레임 하단) | **AVCA ON**, 자세 융합 사용 | 기울기(Roll/Pitch) 안정 추정, 전복 판정 |
| IMU #2 (손잡이 또는 프레임) | **AVCA OFF**, 최대 출력 주기, 광대역 | **충격(Impact) 전용 채널** |

이렇게 하면 "IMU 2개의 목적"이라는 문서상 미설명 항목이 자연스럽게 해소되고, 심사에서도 설계 의도가 선다.
최소한 **AVCA ON/OFF 두 조건에서 같은 충격 이벤트의 피크 값을 비교한 실험 데이터**는 반드시 확보할 것.

### 3.2 [치명 · 최우선 확인] 가속도계 측정 범위(Full-Scale Range)

임계값을 **2.8 ~ 3.5 g**로 잡았는데, **가속도계 FSR이 ±2 g 또는 ±4 g로 설정돼 있으면 그 값에 도달하지 못하거나
클리핑된다.** 이 경우 낙상은 **영원히 감지되지 않는다.**

- 보행기 전복은 금속 프레임이 지면에 직접 부딪히는 사건이라, 인체 착용형(2.5~3 g)보다 **훨씬 높은 피크**
  (수 g ~ 십수 g)가 나올 수 있다.
- → **EBIMU-9DOFV6의 가속도계 FSR을 데이터시트에서 확인하고 최소 ±16 g로 설정할 것.**
- → 설정 명령 시퀀스에 FSR 항목이 **아예 없다.** 반드시 추가.

### 3.3 [중요] 출력 주기 100 Hz(`<sor10>`)로 피크를 놓칠 수 있다

강체 충돌의 가속도 전이는 수 ms 단위로 끝난다. 100 Hz(10 ms 간격)는 **피크를 지나칠 수 있다**(에일리어싱).

→ **임계값 결정 실험(§6.2) 단계에서는 센서가 지원하는 최대 주기로 로깅**한 뒤,
  "100 Hz로 낮춰도 피크가 유지되는가"를 데이터로 확인하고 운용 주기를 정할 것.
  이 검증 과정 자체가 보고서의 좋은 근거 자료가 된다.

### 3.4 [중요] `<cmf>` 지자기 교정의 함정

- **반드시 최종 조립 상태(금속 프레임 + 배터리 + 벅 컨버터 + 스피커 자석 장착 후)에서 교정할 것.**
  책상 위에서 교정하고 보행기에 달면 하드아이언/소프트아이언 왜곡으로 값이 전부 틀어진다.
- 더 나아가, **낙상 감지에 방위각(Yaw)은 필요 없다.** 지자기 교란(철근 구조물, 전동기, 차량 근처)이
  자세 융합에 섞여 들어와 **Roll/Pitch까지 흔들 위험**이 있으므로, 가능하면 **6축(가속도+자이로) 모드**로
  운용하는 것을 검토하라.
- **설정 저장(flash write) 명령이 시퀀스에 없다.** 대부분의 AHRS는 별도 저장 명령 없이 전원을 끊으면
  설정이 날아간다. 저장 명령을 확인해 시퀀스에 추가하고, **전원 재인가 후 설정이 유지되는지 검증**할 것.
- 설정을 매뉴얼 터미널로 하지 말고 **스크립트(파이썬/셸)로 자동화**하라. 8인이 각자 손으로 치면 보드마다
  설정이 달라져 재현 불가능한 버그가 생긴다.

### 3.5 §4.2 충격 수식 — 맞지만 불충분

`Impact = √(ax² + ay² + az²)` 는 표준적인 SMV(Signal Magnitude Vector)이고, 정지 시 1 g가 나오는 것도 정상이다.
문제는 **단일 임계값만으로는 오탐이 확정적**이라는 것이다. 다음은 전부 3 g를 쉽게 넘긴다:

- 보도블록 턱 넘기 / 과속방지턱
- 보행기를 접거나 들었다 놓기
- 차량 트렁크에 싣기
- 계단 앞에서 들어 올리기

→ **최소 3단 판정 + 취소 유예**로 재설계 (구현은 §4.3 코드 참조):

1. **(선택) 자유낙하:** `|a| < 0.5 g` 가 100 ms 이상
2. **충격 피크:** `|a| ≥ Threshold`
3. **사후 자세 확인:** 충격 후 Roll/Pitch가 기준 대비 **60° 이상 틀어진 상태로 2~3초 지속**
4. **취소 유예:** 부저를 울리며 **15~30초 카운트다운** → 사용자가 취소 버튼을 누르면 전송 취소

문서 §4.2의 "**또는** 급격한 기울기 변화"는 **"또는"이 아니라 "그리고(AND)"** 여야 오탐이 잡힌다.

---

## 4. 코드 리뷰 — `tipover_monitor_node.py`

전체 구조와 QoS 적용은 정확하다. 다만 아래는 **의견이 아니라 확정적인 결함**이다.

### 4.1 결함 목록

| # | 심각도 | 위치 | 내용 |
| :-- | :--- | :--- | :--- |
| ① | **치명** | `imu_callback` | **단위 불일치.** ROS 2 `sensor_msgs/Imu.linear_acceleration`은 **표준상 m/s²** 다. 드라이버가 표준을 지키면 정지 상태 SMV가 **9.81**이 되어 `>= 3.0` 조건이 **항상 참** → 10초마다 영구 오알림. 반대로 g 단위를 그 필드에 담으면 rviz·robot_localization 등 표준 노드와 전부 어긋난다. |
| ② | **치명** | `send_discord_alert` | **Google Maps 링크가 깨져 있다.** `f"[https://...maps?q=](https://...maps?q=){lat},{lon}"` 는 마크다운 편집기에서 자동 링크화되며 망가진 문자열이다. 보호자가 받는 링크가 동작하지 않는다. → `f"https://www.google.com/maps?q={lat},{lon}"` |
| ③ | **치명** | `current_lat/lon = 0.0` | **GPS 미수신 상태에서 사고가 나면 (0, 0)이 전송된다.** 보호자는 아프리카 서쪽 대서양 좌표를 받는다. `NavSatFix.status.status == -1`(NO_FIX) 검사와 **좌표 신선도(age)** 검사가 없다. |
| ④ | **치명** | `imu_callback` → `requests.post` | **콜백 안에서 블로킹 HTTP 호출.** 문서 §5.1은 "비동기 전송"이라고 적었지만 코드는 동기다. 단일 스레드 executor에서 최대 3초(+DNS 지연은 `timeout`에 포함되지 않아 그 이상) 동안 **노드 전체가 멈춘다.** 그 사이 IMU·GPS 콜백이 중단되고 depth=1이라 데이터가 버려지며, **부저 발행도 지연**된다. 사고 순간에 가장 멈추면 안 되는 코드다. |
| ⑤ | 높음 | `buzzer_pub` | **부저를 켜기만 하고 끄는 코드가 없다.** `True` 발행 후 `False`가 영원히 오지 않는다. 종료 조건/타임아웃/취소 입력 필요. |
| ⑥ | 높음 | 전체 | **취소(cancel) 경로가 없다.** 임계값 초과 → 즉시 보호자 전송. 오탐 1회로 서비스 신뢰가 무너진다. |
| ⑦ | 높음 | `datetime.now()` | 시스템 로컬 시각. Pi 5에 RTC 배터리가 없고 오프라인이면 **부팅 시각이 틀린다.** → GNSS UTC 또는 RTC 배터리 사용, 그리고 UTC/타임존 명시. |
| ⑧ | 중간 | 예외 처리 | 전송 실패 시 로그만 남기고 **끝난다.** 재시도·로컬 큐잉이 없어, LTE가 잠깐 끊긴 순간의 사고는 **영영 통보되지 않는다.** |
| ⑨ | 중간 | `alert_cooldown` | 10초 쿨다운은 "연속 충격 = 새 사고"로 취급한다. 실제로는 **하나의 사고를 여러 번 알리는** 결과가 되기 쉽다. 상태 머신(정상/의심/확정/복구)으로 대체 권장. |
| ⑩ | **보안** | `webhook_url` 파라미터 | Webhook URL을 **ROS 파라미터로 선언**하면 같은 DDS 도메인의 누구나 `ros2 param get`으로 읽을 수 있고, `ros2 bag`·파라미터 덤프·launch 파일에 남는다. → **환경변수 또는 권한 제한된 설정파일**로 읽고, 로그에 절대 출력하지 말 것. 소스의 플레이스홀더가 실제 URL로 바뀐 채 커밋되는 사고가 가장 흔하다. |
| ⑪ | 낮음 | `status_code == 204` | 204만 성공 처리. 200/429(rate limit) 등 미처리. `raise_for_status()` + 429의 `Retry-After` 처리 권장. |
| ⑫ | 낮음 | `main()` | `KeyboardInterrupt` 처리와 `rclpy.shutdown()` 중복 호출 가드가 없다. |

### 4.2 스레드 안전 주의

④를 고치려고 `MultiThreadedExecutor`로 바꾸면, `current_lat/lon`을 두 스레드가 만지게 되므로
**콜백 그룹 분리 또는 락**이 필요해진다. 아래 코드는 **워커 스레드 + 큐** 방식으로 그 문제를 피한다.

### 4.3 개선 코드 (구조 예시)

```python
import os, math, json, time, queue, threading
from datetime import datetime, timezone

import rclpy, requests
from rclpy.node import Node
from rclpy.qos import QoSProfile, ReliabilityPolicy, HistoryPolicy
from sensor_msgs.msg import Imu, NavSatFix, NavSatStatus
from std_msgs.msg import Bool

G = 9.80665

class AccidentMonitorNode(Node):
    def __init__(self):
        super().__init__('tipover_monitor_node')

        # --- 파라미터 (임계값은 실험으로 확정, 전부 YAML 외부화) ---
        self.declare_parameter('crash_threshold_g', 3.0)
        self.declare_parameter('tilt_threshold_deg', 60.0)
        self.declare_parameter('tilt_hold_sec', 2.0)
        self.declare_parameter('cancel_window_sec', 20.0)
        self.declare_parameter('gps_max_age_sec', 5.0)
        self.declare_parameter('imu_in_g', False)   # 드라이버가 g로 내보내면 True

        p = self.get_parameter
        self.th_g       = p('crash_threshold_g').value
        self.th_tilt    = p('tilt_threshold_deg').value
        self.tilt_hold  = p('tilt_hold_sec').value
        self.cancel_win = p('cancel_window_sec').value
        self.gps_max_age= p('gps_max_age_sec').value
        self.imu_in_g   = p('imu_in_g').value

        # [보안] Webhook은 ROS 파라미터가 아닌 환경변수로만 취득 (§6)
        self.webhook_url = os.environ.get('WALKER_WEBHOOK_URL', '')
        if not self.webhook_url:
            self.get_logger().error('WALKER_WEBHOOK_URL 미설정 — 외부 알림 비활성화')

        # --- 상태 ---
        self.state = 'NORMAL'          # NORMAL → SUSPECT → CONFIRMED
        self.fix = None                # (lat, lon, status, stamp)
        self.impact_peak = 0.0
        self.suspect_t = 0.0
        self.tilt_start = None

        sensor_qos = QoSProfile(reliability=ReliabilityPolicy.BEST_EFFORT,
                                history=HistoryPolicy.KEEP_LAST, depth=20)  # depth 상향(§2.3)
        alert_qos  = QoSProfile(reliability=ReliabilityPolicy.RELIABLE,
                                history=HistoryPolicy.KEEP_LAST, depth=10)

        self.create_subscription(Imu, '/imu/data', self.imu_cb, sensor_qos)
        self.create_subscription(NavSatFix, '/gps/fix', self.gps_cb, sensor_qos)
        self.create_subscription(Bool, '/hardware/cancel_button', self.cancel_cb, alert_qos)
        self.buzzer_pub = self.create_publisher(Bool, '/hardware/buzzer_alert', alert_qos)

        # [핵심] 전송은 별도 워커 스레드 — 콜백을 절대 블로킹하지 않는다 (결함 ④)
        self.txq = queue.Queue(maxsize=64)
        threading.Thread(target=self._sender_loop, daemon=True).start()

        self.create_timer(0.1, self.tick)   # 상태 머신 주기 평가

    # ---------- 콜백 ----------
    def gps_cb(self, msg: NavSatFix):
        if msg.status.status == NavSatStatus.STATUS_NO_FIX:
            return                                  # 결함 ③: 무효 좌표 배제
        self.fix = (msg.latitude, msg.longitude, msg.status.status, self.now())

    def cancel_cb(self, msg: Bool):
        if msg.data and self.state == 'SUSPECT':
            self.get_logger().info('사용자 취소 — 알림 미발송')
            self.reset()

    def imu_cb(self, msg: Imu):
        a = msg.linear_acceleration
        mag = math.sqrt(a.x**2 + a.y**2 + a.z**2)
        # 결함 ①: ROS 표준은 m/s². 드라이버 설정에 맞춰 명시적으로 g로 정규화
        impact_g = mag if self.imu_in_g else mag / G

        roll, pitch = self.quat_to_rp(msg.orientation)
        tilt = max(abs(roll), abs(pitch))

        if self.state == 'NORMAL' and impact_g >= self.th_g:
            self.state = 'SUSPECT'
            self.impact_peak = impact_g
            self.suspect_t = self.now()
            self.tilt_start = None
            self.buzzer(True)
            self.get_logger().warn(f'충격 감지 {impact_g:.2f}g — {self.cancel_win:.0f}초 취소 대기')

        elif self.state == 'SUSPECT':
            self.impact_peak = max(self.impact_peak, impact_g)
            # 사후 자세 유지 확인 (§3.5-3)
            if tilt >= self.th_tilt:
                self.tilt_start = self.tilt_start or self.now()
            else:
                self.tilt_start = None

    # ---------- 상태 머신 ----------
    def tick(self):
        if self.state != 'SUSPECT':
            return
        elapsed = self.now() - self.suspect_t
        tilted = self.tilt_start and (self.now() - self.tilt_start) >= self.tilt_hold

        if not tilted and elapsed > self.tilt_hold + 1.0:
            self.get_logger().info('자세 정상 — 단순 충격으로 판단, 알림 미발송')
            self.reset()                              # 결함 ⑤·⑥·⑨ 해소
            return

        if tilted and elapsed >= self.cancel_win:
            self.state = 'CONFIRMED'
            self.enqueue_alert()

    def enqueue_alert(self):
        lat = lon = None; qual = 'NO_FIX'; age = None
        if self.fix:
            lat, lon, st, t = self.fix
            age = self.now() - t
            if age <= self.gps_max_age:
                qual = {0: 'FIX', 1: 'SBAS', 2: 'GBAS/RTK'}.get(st, str(st))
            else:
                qual = f'STALE({age:.0f}s)'           # 결함 ③: 신선도 표기
        try:
            self.txq.put_nowait({
                'utc': datetime.now(timezone.utc).isoformat(timespec='seconds'),
                'impact_g': round(self.impact_peak, 2),
                'lat': lat, 'lon': lon, 'quality': qual, 'age_s': age,
            })
        except queue.Full:
            self.get_logger().error('전송 큐 포화')

    # ---------- 전송 워커 (재시도 + 오프라인 큐잉, 결함 ⑧) ----------
    def _sender_loop(self):
        while True:
            item = self.txq.get()
            for attempt in range(6):                  # 지수 백오프 재시도
                if self._post(item):
                    break
                time.sleep(min(2 ** attempt, 60))
            self.txq.task_done()

    def _post(self, d) -> bool:
        if not self.webhook_url:
            return False
        loc = (f"위도 {d['lat']:.7f} / 경도 {d['lon']:.7f}"
               if d['lat'] is not None else '⚠ 위치 미확보')
        link = (f"https://www.google.com/maps?q={d['lat']},{d['lon']}"   # 결함 ② 수정
                if d['lat'] is not None else '-')
        payload = {"content": "🚨 **[보행기 사고 감지]**",
                   "embeds": [{"title": "사고 정보", "color": 16711680, "fields": [
                       {"name": "발생 시각(UTC)", "value": d['utc'], "inline": True},
                       {"name": "충격량", "value": f"{d['impact_g']} G", "inline": True},
                       {"name": "측위 품질", "value": d['quality'], "inline": True},
                       {"name": "위치", "value": loc, "inline": False},
                       {"name": "지도", "value": link, "inline": False}]}]}
        try:
            r = requests.post(self.webhook_url, json=payload, timeout=5.0)
            if r.status_code == 429:
                time.sleep(float(r.headers.get('Retry-After', 5)))
                return False
            return r.ok or r.status_code == 204
        except Exception as e:
            self.get_logger().error(f'웹훅 전송 실패(재시도 예정): {e}')   # URL은 로그에 남기지 않음
            return False

    # ---------- 유틸 ----------
    def buzzer(self, on: bool):
        m = Bool(); m.data = on; self.buzzer_pub.publish(m)

    def reset(self):
        self.state = 'NORMAL'; self.impact_peak = 0.0
        self.tilt_start = None; self.buzzer(False)     # 결함 ⑤: 반드시 끈다

    def now(self):
        return self.get_clock().now().nanoseconds / 1e9

    @staticmethod
    def quat_to_rp(q):
        roll = math.atan2(2*(q.w*q.x + q.y*q.z), 1 - 2*(q.x*q.x + q.y*q.y))
        s = max(-1.0, min(1.0, 2*(q.w*q.y - q.z*q.x)))
        pitch = math.asin(s)
        return math.degrees(roll), math.degrees(pitch)
```

> **주의:** 위 코드는 구조 예시다. 임계값(`crash_threshold_g`, `tilt_threshold_deg`)은
> §6.2의 실측 데이터로 확정해야 하며, **취소 버튼 토픽 `/hardware/cancel_button`은
> 현재 BOM에 없는 하드웨어**다 (§7-3).

---

## 5. 전원 및 기구 검토 (§6.1)

### 5.1 잘한 점

**1학기는 개별 정격 AC 어댑터, 2학기에 배터리 통합**이라는 단계 분리는 정확한 판단이다.
전원 노이즈와 S/W 버그가 뒤엉키면 디버깅 비용이 폭증하므로, 변수를 차단한 것은 옳다.

단, **"AC 어댑터에서 되던 것이 배터리에서 안 되는" 리스크**를 일정에 반영하라.
전압 강하, 리플, 그라운드 루프 문제는 배터리 전환 시점에 새로 터진다.
→ **2학기 첫 2주를 "전원 전환 회귀 검증" 기간으로 별도 배정**할 것.

### 5.2 [치명] Raspberry Pi 5의 5A 전원 협상 문제

**Pi 5는 USB-C 전원공급기가 5A 지원임을 PD로 통보해야만 USB 포트 전류 한도를 풀어준다.**
직접 만든 벅 컨버터로 5V를 밀어 넣으면 PD 협상이 없으므로, Pi 5는 **USB 주변장치 전체 합계를 약 600 mA로 제한**한다.

그런데 이 시스템에는 **LiDAR ×2(USB) + ZED-F9P(USB) + LTE 동글(USB)** 이 붙을 예정이다. **반드시 걸린다.**

대응 (택1, 검증 필수):
- `/boot/firmware/config.txt` 에 **`usb_max_current_enable=1`** 을 설정해 제한 해제
  (이 경우 전원 여유를 반드시 확보해야 하며, 부족 시 리부트 루프가 난다)
- **PD 트리거 보드**를 써서 정식으로 5V/5A를 협상
- **GPIO 5V 핀으로 급전** (보호회로를 우회하므로 과전압·역전압 보호를 외부에 직접 구현해야 함)
- 또는 **셀프 파워 USB 허브**로 LiDAR·모뎀을 분리 급전 (가장 안전, 무게·부피 증가)

### 5.3 배터리 사양 검토

- **3S(9.0~12.6 V) vs 4S(12.0~16.8 V):** Jetson Orin Nano 개발키트 입력은 9~20 V DC다.
  **3S는 방전 말기 9.0 V까지 떨어져 부하 시 전압 강하와 겹치면 하한을 침범할 수 있다.**
  → **4S 권장**, 또는 3S를 쓴다면 중간에 승압/안정화 단을 두고 저전압 컷오프를 여유 있게 잡을 것.
- **문서에 용량(Wh)·목표 구동시간·무게가 없다.** 다음을 §6.1에 채워라:

  | 부하 | 추정 소비전력 |
  | :--- | :--- |
  | Jetson Orin Nano Super | 7 ~ 25 W |
  | Raspberry Pi 5 (8GB) | 5 ~ 10 W |
  | LiDAR ×2 + F9P/능동안테나 + IMU ×2 + 부저/LED + 허브 | 5 ~ 12 W |
  | **합계** | **약 20 ~ 47 W** |

  → 4시간 목표 시 **약 100~190 Wh**, 무게 **1~2 kg**. 실측 후 확정할 것.

### 5.4 [치명] 안전 항목 — 자작 리튬팩

"고방전 커스텀 리튬이온 배터리 팩 제작"은 **고령자가 체중을 싣는 기구에 장착되는 자작 배터리**다.

- **BMS(과충전/과방전/과전류/셀 밸런싱) 필수**, 인라인 퓨즈 필수, 셀 간 스팟 용접 품질 관리.
- 진동 환경 → 셀 홀더·배선 고정, 단락 방지 절연, 케이스 내 열 배출.
- **자작 팩은 KC 인증 대상이 아니므로 실사용 배포가 불가하다.** 보고서에
  **"연구·시연 목적 한정, 상용화 시 인증된 팩으로 대체"** 를 명시할 것.
- 교내 리튬 배터리 취급·보관 규정 확인.

### 5.5 [누락] 안전 종료(Graceful Shutdown)

Jetson과 Pi를 **전원 스위치로 그냥 끊으면 파일시스템이 손상된다.** 학기 말에 SSD가 부팅 불가가 되는
사고가 실제로 자주 발생한다.
→ **종료 버튼 → OS 정상 종료 → 이후 전원 차단** 시퀀스를 회로로 구현할 것
(감시용 소형 MCU 또는 지연 차단 회로 + 슈퍼커패시터).
→ 배터리 잔량 표시와 **잔량 5% 자동 안전 종료**도 함께.

### 5.6 [누락 · 중요] 무게중심과 전도 안정성

문서 전체에 **기구 설계와 무게중심에 대한 언급이 없다.**

**보행기 상단에 1~2 kg의 배터리와 보드를 얹으면 무게중심이 올라가 전복 위험이 오히려 커진다.
낙상을 막으려는 장치가 낙상을 유발하면 과제의 전제가 무너진다.**

→ **중량물(배터리·보드)은 최하단, 바퀴 축 근처에 배치.**
→ 보고서에 **장착 전/후 무게중심 높이와 전도각(tipping angle) 비교**를 넣어라.
  이 분석 한 장이 "안전을 실제로 고민한 팀"이라는 인상을 결정한다.

---

## 6. 보안 (Security)

| 항목 | 위험 | 대책 |
| :--- | :--- | :--- |
| **Webhook URL / 봇 토큰** | 유출 시 제3자가 **고령자의 실시간 위치를 수신**하거나 **위장 알림을 발송**할 수 있다. 사실상 스토킹 도구가 된다. | 코드의 `'YOUR_DISCORD_WEBHOOK_URL_HERE'` 자리에 실제 URL을 넣고 커밋하는 사고가 가장 흔하다. **환경변수/권한 제한 파일**로 분리, `.gitignore` 등록, 이미 노출됐다면 **즉시 재발급**. 발표자료·시연 화면 캡처·로그에도 찍히지 않게 할 것. |
| **ROS 파라미터** | `ros2 param get` 으로 같은 도메인 누구나 URL 조회 가능. `ros2 bag`·파라미터 덤프에도 기록됨 | 비밀값은 **ROS 파라미터로 선언하지 말 것** (§4.1-⑩) |
| **ROS 2 / DDS** | DDS는 기본적으로 **인증·암호화가 없다.** 같은 망의 누구나 `/hardware/buzzer_alert`에 가짜 메시지를 발행하거나 사고 이벤트를 구독할 수 있다 | 보드 간 통신은 **유선 직결로 격리**, Wi-Fi로 DDS가 나가지 않도록 인터페이스 바인딩(§2.1). 여유가 되면 **SROS2(DDS-Security)** 를 확장 과제로 |
| **`ROS_DOMAIN_ID=30`** | 같은 실습실 타 팀과 겹치면 **유령 노드·토픽 충돌** | 팀 고유값으로 관리하고 문서에 기록 (30은 흔히 쓰이는 값이라 충돌 가능) |
| **원격 접속 (SSH/VNC)** | 기본 계정(`ubuntu/ubuntu`, `pi/raspberry`), 비밀번호 로그인 | **키 기반 인증만 허용**, 비밀번호 로그인 비활성화, 기본 계정 변경, VNC는 필요 시에만 |
| **NTRIP 계정** | 국토지리정보원 VRS 계정 ID/PW가 소스에 하드코딩되기 쉬움 | 동일하게 환경변수 처리. **VRS는 자신의 NMEA 위치를 서버로 올린다** → 위치가 외부로 나간다는 점을 개인정보 항목에 명시 |
| **저장 로그** | 이동 궤적 로그 = 생활 패턴 전체 | 보관 기간 한정 + 자동 삭제, 반출 시 익명화, SD/SSD 분실 대비 |
| **전송 구간** | 평문/인증서 검증 우회 | HTTPS만 사용, `verify=False` 금지 |

---

## 7. 즉시 처리 목록 (Blocker)

> 순서대로. 1~4는 **착수 2주 내**, 5~8은 **1학기 중반 전**.

1. **LTE 모뎀 + 데이터 SIM 조달 확정.** §6.2의 NTRIP VRS와 Discord Webhook은 **둘 다 로버의 상시 인터넷이 필요**한데
   BOM에 회선이 없다. 현재 계획의 최대 구멍이다.
   (LTE HAT 또는 USB 동글 + 요금제. 시연 한정 테더링으로 간다면 "실사용성 한계"를 보고서에 명시.)
2. **가속도계 FSR 확인 및 ±16 g 설정.** ±2/±4 g면 3 g 임계값은 **원리적으로 도달 불가**다. (§3.2)
3. **취소 버튼 · SOS 버튼 · 안전 종료 버튼을 BOM에 추가.** (§4.1-⑥, §5.5)
4. **ROS 2 배포판 통일 결정.** Jetson은 JetPack 6(22.04) → **Humble**. 반면 **Raspberry Pi 5는 Ubuntu 22.04를
   공식 지원하지 않는다**(Pi 5 지원은 23.10 이후). 24.04를 깔면 **Jazzy**가 되어 배포판이 갈리고,
   **ROS 2는 배포판 간 통신을 보장하지 않는다.**
   → 권고: **Pi 5는 Ubuntu 24.04 또는 Pi OS 64bit + Docker로 Humble 컨테이너 실행**해 양쪽을 Humble로 통일
   (`--network host`, GPIO/시리얼은 `--device`). **1주차에 확정할 것.** 나중에 바꾸면 8명 전원이 환경을 다시 만든다.
5. **실외 태양광 아래 LiDAR 실측.** ToF는 직사광에서 유효거리·정확도가 급락한다. 본 과제는 RTK를 쓰는
   **실외** 시나리오이므로, 실내 검증만 하고 실외에서 처음 켰다가 실패하는 사고가 이 조합에서 가장 흔하다.
   맑은 날 정오, 아스팔트·보도블록 위에서 조기 실측할 것. 결과에 따라 초음파 병용 또는 시나리오 조정.
6. **Pi 5 USB 전류 제한 해소 방안 결정.** (§5.2) — USB 장치가 4개까지 늘어난다.
7. **코드 결함 ①②③④ 수정.** (§4.1) — 특히 단위(m/s² vs g)와 GPS (0,0)은 시연 사고로 직결된다.
8. **무게중심·전도각 검토 및 저중심 배치 확정.** (§5.6)

### 부수 확인 사항

- **CygLiDAR 인터페이스 표기 오류 의심.** §2.2 표에 "USB / 이더넷"으로 되어 있으나 해당 제품군은 통상
  **USB(CDC 시리얼) 또는 UART**다. 데이터시트 확인 필요.
- **LiDAR 2대의 `/dev/ttyUSB*` 번호는 부팅마다 뒤바뀐다.** 반드시 `udev` 규칙으로 시리얼번호 기반
  고정 심볼릭 링크(`/dev/lidar_left`, `/dev/lidar_right`)를 만들 것. 좌우가 바뀌면 점군이 반대로 합쳐진다.
- **제조사 ROS 2 드라이버의 Humble 지원 및 2대 동시 구동**(네임스페이스·포트 파라미터화) 가능 여부.
- **Pi 5 UART 2채널 활성화**(디버그 콘솔 비활성 + 오버레이) — EBIMU 2대 연결에 필요.
- **CygLiDAR 2D/3D 모드별 화각·유효거리 확정.** "각 120°, 합계 240°"는 겹침이 0일 때만 성립하며,
  실제로는 10~20° 겹치게 배치하고 **유효 화각 ~220°** 로 보수적으로 기술하는 편이 안전하다.
  동종 ToF 2대의 시야가 겹치면 **상호 간섭(crosstalk)** 가능성도 확인 대상이다.
- **"턱 걸림" 감지 경로 부재.** 기획 배경(§1)에 명시돼 있으나, 전방을 수평으로 보는 LiDAR는
  **바닥 단차를 볼 수 없다.** 아래로 10~20° 틸트 + 지면 평면(RANSAC) 추정으로 음(−)/양(+) 편차를
  구분하거나, 바닥 방향 거리센서를 추가해야 한다. **기획과 설계의 불일치이므로 둘 중 하나를 수정할 것.**
- **"AI 비전 처리"(§2.1)에 대응하는 카메라가 BOM에 없다.** 문구 삭제 또는 카메라 추가로 정리.
- **3D SLAM의 용도가 산출물에 없다.** 확장 과제로 강등 권장 (§8).

---

## 8. 여전히 비어 있는 문서 챕터

- **정량 요구사항 명세** — 지금 문서에는 "무엇을 어디까지 증명할 것인가"가 없다. 예시:

  | ID | 요구사항 | 목표 | 측정 방법 |
  | :-- | :-- | :-- | :-- |
  | FR-01 | 전방 장애물 경고 | 유효거리 내 탐지율 ≥ 95% | 10지점 × 20회 |
  | FR-02 | 낙상 감지 | 재현 낙상 20회 중 ≥ 18회 탐지, 오경보 ≤ 1회/2h | 시나리오 시험 |
  | NFR-01 | 알림 지연 | 충격 → 메신저 도착 ≤ 10 s | 타임스탬프 로그 |
  | NFR-02 | 위치 정확도 | RTK FIX 시 ≤ 0.1 m, 미확보 시 품질코드 동봉 | 기준점 대조 |
  | NFR-03 | 연속 구동 | ≥ 4 h | 실부하 방전 시험 |

- **검증 계획** — 단위 → HIL → 실외 필드. §6.2의 데이터 로깅 계획을
  **양성/음성 샘플 수, 평가지표(오탐률·미탐률)** 까지 구체화할 것.
  **실제 고령자를 대상으로 한 낙상 재현 실험은 하지 말 것.** 더미·매트 위 재현으로 대체하고,
  음성 샘플은 정상 보행 / 턱 넘기 / 급정지 / 접어서 들기 / 차량 적재로 구성.
- **FMEA(실패 모드 분석)** — 인터넷 두절 / Jetson 다운 / LiDAR 무응답 / GNSS 미확보 / 배터리 방전 각각의
  대응. 핵심 원칙: **인터넷이 끊겨도 현장 부저는 반드시 크게 울려야 한다**(주변인 도움 유도).
  그리고 **어떤 센서도 "조용히 죽게" 두지 말 것** — 타임아웃 감시 후 사용자에게 고지.
- **일정(WBS)·역할 분담** — **8인 팀인데 분담이 문서에 전혀 없다.**
  제안: ① LiDAR 드라이버·캘리브레이션 2인 ② 점군 처리·경고 로직 2인 ③ GNSS/RTK·NTRIP·통신 1인
  ④ IMU·낙상 알고리즘 2인 ⑤ 전원·기구·조립 1인 (문서·시험은 전원 공동)
- **예산(BOM 단가·합계)과 조달 일정** — **LiDAR 2대와 ZED-F9P는 리드타임이 길다.**
  발주 지연은 캡스톤 일정 붕괴의 1위 원인이다.
- **법적·윤리적 고려** — 의료기기 아님 명시(면책), 위치정보 수집·이용 동의 절차 설계,
  보관 기간, 시연 영상 비식별화.
- **사용자 인터페이스 설계** — **노인성 난청은 고주파부터 손실된다.** 흔한 4 kHz 부저는 대상 사용자에게
  들리지 않을 수 있다. **1~3 kHz, 단속음, 근거리 80~85 dB** + **손잡이 진동 병행**을 권장.
  경보 피로 방지(히스테리시스, 정지 시 경고 중단)도 §6.2의 3단계 로직에 포함할 것.

---

## 9. 권고 우선순위 (범위 조정안)

| 단계 | 내용 | 비고 |
| :--- | :--- | :--- |
| **P0 (반드시 완성)** | IMU 낙상 감지 → 취소 카운트다운 → GNSS 좌표 + 측위 품질 + 충격량 메신저 전송 | **이것만 완벽하면 과제는 성립한다.** Pi 단독 구현이므로 **Jetson 일정과 분리**할 것 |
| **P1** | LiDAR **1대** 기반 전방 장애물 3단계 경고 | 1대로 완성 후 2대로 확장 |
| **P2** | 듀얼 LiDAR 외부 캘리브레이션 및 240° 융합 | 캘리브레이션 절차 문서화 자체가 성과물 |
| **P3** | 바닥 단차(턱) 감지 | 기획 배경과의 정합성 회복 |
| **P4 (확장)** | 3D SLAM, AI 비전, SROS2 | 시간이 남으면. 없어도 과제는 완결됨 |

**P0를 Jetson과 무관하게 독립 동작시키는 것**이 이 프로젝트 최대의 리스크 관리다.
**발표 전날 Jetson이 부팅되지 않아도 시연이 가능해야 한다.**

---

### 검토자 메모

- QoS 정책 분리, 단계적 전원 전략, NTRIP 사전 식별, 캘리브레이션 과제화 — 계획의 **전략 층위는
  캡스톤 평균을 상회한다.** 지적 사항 대부분은 "방향이 틀렸다"가 아니라
  **"이 방향대로 가면 다음 지점에서 걸린다"** 는 종류다.
- 다만 **코드는 아직 동작하는 코드가 아니다.** §4.1의 ①②③④는 취향이 아니라 결함이며,
  특히 **단위 문제(①)는 "부저가 계속 울리는" 형태로, GPS (0,0) 문제(③)는 "엉뚱한 좌표 전송" 형태로**
  반드시 드러난다. 지금 잡으면 30분, 시연 전날 잡으면 하룻밤이다.
- 다음 개정에서 **§8의 정량 요구사항 표**를 먼저 채우고, 그 기준으로 §9의 범위를 팀 내 합의할 것을 권한다.

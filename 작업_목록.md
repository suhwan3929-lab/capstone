# Raspberry Pi 5 · Jetson Orin Nano 배포 작업 목록

> 목표: **Jetson은 LiDAR 처리만**, **Raspberry Pi는 전도 감지·경보·GPS·보호자 알림**을 맡긴다.
> 두 보드는 유선 Ethernet의 UDP 9100으로 연결한다. Pi의 안전 기능은 Jetson이나 UI가 멈춰도 계속 동작해야 한다.

---

## 0. 배포 전에 PC에서 할 일

- [ ] 프로젝트를 GitHub에 올린다. 현재 원격 저장소만 연결되어 있으므로, 최초 커밋과 푸시가 먼저 필요하다.

  ```powershell
  cd "D:\nas\유수환\26-2 capstone"
  git add .
  git commit -m "Initial walker safety system"
  git push -u origin main
  ```

- [ ] `capstone_code/config_local.py.example`을 참고해 **보드별 `config_local.py`를 따로 준비**한다. 이 파일은 Git에 올리지 않는다.
- [ ] IMU 수신기, F9P, LiDAR 좌·우의 USB 시리얼 번호를 기록한다.
- [ ] 다음 값을 확정한다.

  | 항목 | 권장값 |
  | --- | --- |
  | Pi 주소 | `192.168.10.2` |
  | Jetson 주소 | `192.168.10.1` |
  | Pi ↔ Jetson 연결 | 유선 Ethernet |
  | Python | 3.11 권장 |
  | Jetson ROS 2 | JetPack 6 / Ubuntu 22.04면 Humble |

---

## 1. Raspberry Pi 5 — 넣을 폴더와 역할

### 1-1. Pi에 복사할 내용

Pi의 실제 설치 경로는 `/opt/walker`로 정한다.

| PC 원본 | Pi 대상 | 용도 |
| --- | --- | --- |
| `capstone_code/*.py` | `/opt/walker/` | 안전 감시, GPS, 알림, UI |
| `capstone_code/sensors/` | `/opt/walker/sensors/` | IMU·GPS·Jetson UDP 수신 |
| `capstone_code/systemd/` | `/opt/walker/systemd/` | 서비스 유닛 원본 |
| `capstone_code/requirements.txt` | `/opt/walker/requirements.txt` | Python 의존성 |
| `capstone_code/config_local.py` | `/opt/walker/config_local.py` | **Pi 전용 포트·LiDAR 설정** |
| `capstone_code/docs/` (선택) | `/opt/walker/docs/` | 현장 참고 문서 |

복사하지 않을 것: `.venv/`, `.python/`, `__pycache__/`, `logs/`, `legacy/`, PC용 COM 포트가 적힌 `config_local.py`.

### 1-2. Pi OS 기본 설정

- [ ] Raspberry Pi OS 64-bit를 설치하고 인터넷 연결 및 업데이트를 한다.
- [ ] I²C를 사용한다면 `sudo raspi-config`에서 **I2C Interface를 활성화**한다.
- [ ] 시스템 시간대를 `Asia/Seoul`로 맞추고 NTP가 동작하는지 확인한다.
- [ ] 보드 간 고정 IP를 설정한다. Pi의 이더넷 장치명은 보통 `end0`이므로 Jetson의 `eth0` 설정을 그대로 복사하지 않는다.
- [ ] 시리얼·GPIO·I²C 접근 계정을 만든다.

  ```bash
  sudo useradd -r -s /bin/false -G dialout,gpio,i2c walker || true
  sudo install -d -o walker -g walker /opt/walker /var/log/walker
  ```

### 1-3. USB 포트 고정 (필수)

`/dev/ttyUSB0` 같은 번호는 부팅 순서에 따라 바뀌므로 사용하지 않는다.

- [ ] IMU 수신기와 F9P를 각각 연결한 뒤 시리얼 번호를 확인한다.

  ```bash
  udevadm info -a -n /dev/ttyUSB0 | grep -m1 ATTRS{serial}
  ```

- [ ] `/etc/udev/rules.d/99-walker.rules`에 규칙을 작성한다.

  ```text
  SUBSYSTEM=="tty", ATTRS{serial}=="<IMU 수신기 시리얼>", SYMLINK+="walker_imu"
  SUBSYSTEM=="tty", ATTRS{serial}=="<F9P 시리얼>",       SYMLINK+="walker_gps"
  ```

- [ ] 규칙을 반영하고 고정 링크를 확인한다.

  ```bash
  sudo udevadm control --reload-rules
  sudo udevadm trigger
  ls -l /dev/walker_imu /dev/walker_gps
  ```

### 1-4. Pi 전용 `config_local.py`

`/opt/walker/config_local.py`에 최소한 다음을 넣는다.

```python
IMU_PORT = "/dev/walker_imu"
GPS_PORT = "/dev/walker_gps"
LIDAR_SOURCE = "udp"
```

- [ ] IMU를 수평 정지 상태로 둔 뒤 장착각을 보정한다.

  ```bash
  cd /opt/walker
  /opt/walker/venv/bin/python safety_monitor.py --calibrate
  ```

- [ ] 출력된 `MOUNT_ROLL_OFFSET_DEG`, `MOUNT_PITCH_OFFSET_DEG`를 `config_local.py`에 기록한다.
- [ ] IMU 가속도계 측정 범위(FSR)가 **±16 g**인지 제조사 도구/명령으로 확인하고, 전원 재가동 후에도 유지되는지 검증한다.

### 1-5. 소스 복사와 Python 환경

GitHub에 최초 푸시 후 Pi에서 실행한다.

```bash
sudo git clone https://github.com/suhwan3929-lab/capstone.git /opt/walker
sudo chown -R walker:walker /opt/walker
sudo -u walker python3 -m venv /opt/walker/venv
sudo -u walker /opt/walker/venv/bin/pip install -r /opt/walker/capstone_code/requirements.txt
```

> 현재 서비스 유닛은 작업 경로를 `/opt/walker`로 가정하지만, 저장소 안의 실행 코드는 `capstone_code/`에 있다. 아래 둘 중 하나로 **경로를 통일**해야 한다.
>
> 1. 권장: `/opt/walker/capstone_code`를 작업 경로로 쓰도록 서비스 유닛을 수정한다.
> 2. 또는 `capstone_code/` 내부 내용만 `/opt/walker/`에 복사한다.
>
> 이 목록에서는 1번을 사용한다.

### 1-6. 비밀값과 systemd 등록

- [ ] `/etc/walker/secrets.env`를 만들고 권한을 제한한다.

  ```bash
  sudo install -d -m 700 /etc/walker
  sudo tee /etc/walker/secrets.env >/dev/null <<'EOF'
  WALKER_WEBHOOK_URL=https://discord.com/api/webhooks/...
  NTRIP_USER=...
  NTRIP_PASS=...
  EOF
  sudo chmod 600 /etc/walker/secrets.env
  ```

- [ ] `walker-safety.service`와 `walker-ui.service`의 `WorkingDirectory`, `ExecStart`를 `/opt/walker/capstone_code` 기준으로 맞춘다.
- [ ] 서비스 파일을 등록한다.

  ```bash
  sudo cp /opt/walker/capstone_code/systemd/*.service /etc/systemd/system/
  sudo systemctl daemon-reload
  sudo systemctl enable --now walker-safety
  sudo systemctl enable --now walker-ui
  ```

- [ ] 안전 서비스와 UI를 각각 확인한다.

  ```bash
  systemctl status walker-safety
  systemctl status walker-ui
  journalctl -u walker-safety -f
  ```

---

## 2. Jetson Orin Nano — 넣을 폴더와 역할

### 2-1. Jetson에 필요한 내용

| PC 원본 | Jetson 대상 | 용도 |
| --- | --- | --- |
| Cygbot 공식 ROS 2 드라이버 | `~/walker_ws/src/<벤더_패키지>/` | LiDAR 원본 프레임·토픽 발행 |
| `capstone_code/jetson/lidar_bridge_node.py` | `~/walker_ws/src/walker_lidar/walker_lidar/lidar_bridge_node.py` | 점군/스캔 ROI 요약 → Pi UDP 송신 |
| `capstone_code/jetson/launch/walker_lidar.launch.py` | `~/walker_ws/src/walker_lidar/launch/` | 브리지 실행 설정 |
| `capstone_code/jetson/README.md` | Jetson 또는 팀 문서 | 현장 참고 |

> 주의: 현재 저장소의 `jetson/` 폴더에는 ROS 2 패키지 메타데이터(`package.xml`, `setup.py` 또는 `CMakeLists.txt`)가 없다. 따라서 `ros2 launch walker_lidar ...`를 쓰려면 먼저 `walker_lidar` 패키지 골격을 만들고 위의 노드·launch 파일을 넣어야 한다. 패키지 생성은 Jetson 배포의 선행 작업이다.

### 2-2. Jetson 기본 설정

- [ ] JetPack 6 / Ubuntu 22.04 및 ROS 2 Humble을 준비한다.
- [ ] Python, `colcon`, `rosdep`, `numpy`, `sensor_msgs_py`를 설치한다.
- [ ] Ethernet `eth0`을 `192.168.10.1`로 고정하고 Pi(`192.168.10.2`)와 ping을 확인한다.
- [ ] UDP 9100 송신이 방화벽에 막히지 않는지 확인한다.

### 2-3. LiDAR 포트 고정

- [ ] 좌·우 LiDAR의 시리얼 번호를 확인한다.

  ```bash
  udevadm info -a -n /dev/ttyUSB0 | grep -m1 ATTRS{serial}
  ```

- [ ] `/etc/udev/rules.d/99-walker.rules`에 다음처럼 작성한다.

  ```text
  SUBSYSTEM=="tty", ATTRS{serial}=="<왼쪽 LiDAR 시리얼>", SYMLINK+="lidar_left"
  SUBSYSTEM=="tty", ATTRS{serial}=="<오른쪽 LiDAR 시리얼>", SYMLINK+="lidar_right"
  ```

- [ ] 벤더 드라이버의 좌·우 포트를 `/dev/lidar_left`, `/dev/lidar_right`로 지정한다.

### 2-4. ROS 2 워크스페이스와 브리지 구성

```bash
mkdir -p ~/walker_ws/src
cd ~/walker_ws/src
git clone <Cygbot 공식 ROS 2 드라이버 저장소>
# walker_lidar 패키지 생성 후 lidar_bridge_node.py 및 launch/를 복사
cd ~/walker_ws
rosdep install --from-paths src -y --ignore-src
colcon build --symlink-install
source install/setup.bash
```

- [ ] 벤더 드라이버를 먼저 실행한 뒤 토픽 이름과 메시지 타입을 확인한다.

  ```bash
  ros2 topic list
  ros2 topic info /cygbot_left/scan
  ros2 topic hz /cygbot_left/scan
  ```

- [ ] `walker_lidar.launch.py` 또는 ROS 파라미터에 실제 값을 반영한다.

  - `pi_host:=192.168.10.2`
  - `left_topic`, `right_topic`
  - `msg_type:=LaserScan` 또는 `PointCloud2`
  - LiDAR 모드에 맞는 `min_valid_m`, `max_valid_m`

- [ ] 브리지를 실행한다.

  ```bash
  ros2 launch walker_lidar walker_lidar.launch.py
  ```

---

## 3. Pi ↔ Jetson 통합 검증 순서

- [ ] 양쪽에서 `ping 192.168.10.1` / `ping 192.168.10.2`가 된다.
- [ ] Jetson에서 벤더 LiDAR 토픽이 좌·우 모두 들어온다.
- [ ] Jetson 브리지가 UDP 9100을 Pi(`192.168.10.2`)로 송신한다.
- [ ] Pi에서 `journalctl -u walker-safety -f`를 열고 **`Jetson 링크 연결됨`**을 확인한다.
- [ ] LiDAR 한 대를 뽑아 링크·센서 이상 경고가 나타나는지 확인하고 다시 연결한다.
- [ ] UI를 의도적으로 중지해도 안전 서비스가 계속 동작하는지 확인한다.

  ```bash
  sudo systemctl stop walker-ui
  journalctl -u walker-safety -f
  ```

- [ ] IMU 정상 보행, 턱 통과, 급정지, 전도 모의 시나리오를 각각 시험한다.
- [ ] 부저·진동·LED·취소 버튼·SOS 버튼을 실제 결선으로 시험한다.
- [ ] LTE 연결 후 NTRIP 보정정보 수신과 RTK FIX 상태를 실외에서 확인한다.

---

## 4. 실기 시작 전 최종 확인

- [ ] IMU FSR = ±16 g, 장착각 보정 완료
- [ ] Pi의 IMU/GPS 포트가 `/dev/walker_imu`, `/dev/walker_gps`로 고정됨
- [ ] Jetson의 좌·우 LiDAR 포트가 `/dev/lidar_left`, `/dev/lidar_right`로 고정됨
- [ ] Pi `LIDAR_SOURCE = "udp"`
- [ ] Jetson 브리지의 `pi_host = 192.168.10.2`
- [ ] 웹훅과 NTRIP 계정이 `/etc/walker/secrets.env`에만 존재함
- [ ] `walker-safety`가 부팅 후 자동 시작됨
- [ ] UI·Jetson 중 하나를 멈춰도 Pi의 전도 감지·현장 경보가 계속됨
- [ ] 실외 태양광 조건에서 LiDAR 거리와 단차 감지 성능을 측정함


# 실제 부품 확정에 따른 영향 분석

> 작성일: 2026-08-21
> 계기: 실사용 부품이 계획서와 다름이 확인됨

---

## 0. 변경 요약

| 구분 | 계획서 | **실제** | 영향 |
| :--- | :--- | :--- | :--- |
| IMU | EBIMU-9DOFV6 (3.3V UART 유선) | **EBIMU24GV52** (2.4 GHz **무선**) | **큼** — 인터페이스가 완전히 다름 |
| LiDAR | CygLiDAR D2 (2D+3D) | **CygLiDAR D2** (동일) | 없음 — 단, 유효거리 설정 오류 2건 발견 (§3.2) |
| GPS | ZED-F9P + 헬리컬 안테나 | **UB-ZED03 + 4-Arm Helical 능동 안테나** | 없음 — 계획서 원안과 동일 |

---

## 1. IMU: EBIMU24GV52 — "24G"는 24 g가 아니다

### 1.1 가장 중요한 오해 방지

**`24G` 는 가속도 24 g가 아니라 2.4 GHz 무선을 뜻합니다.**

제품 계보를 보면 EBIMU24GV2 → V3 → V5 → V5.2 로 이어지는 **무선 AHRS 시리즈**이고,
`24G` 는 통신 주파수(2.4 GHz)입니다.

> 이 제품명을 "±24 g 이니까 측정범위 걱정 없다"로 읽으면 안 됩니다.
> **실제 가속도 측정범위는 2 g ~ 16 g 설정 가능**입니다.

### 1.2 결론: FSR 확인 과제는 그대로 유효하다 — 오히려 더 중요해졌다

| | 값 |
| :--- | :--- |
| 가속도 측정범위 | **2 g ~ 16 g (설정 가능)** |
| 자이로 측정범위 | 250 ~ 2000 dps (설정 가능) |
| 내부 갱신 주기 | **1000 Hz** (센서 15개 연결 시 약 85 Hz) |

→ **반드시 ±16 g 로 설정해야 합니다.** 기본값이 ±2 g 또는 ±4 g면
   `IMPACT_THRESHOLD_G = 2.8` 에 도달하지 못하거나 클리핑됩니다.
→ 자이로도 **2000 dps** 로. 전도 순간의 각속도는 수백 dps를 쉽게 넘습니다.

`safety_monitor.py` 의 자동 진단(정지 시 1 g 확인)이 이를 조기에 잡습니다.

### 1.3 좋은 소식 — 충격 피크 에일리어싱 걱정이 줄었다

내부 1000 Hz이고 우리는 센서를 2개만 씁니다.
계획서의 `<sor10>`(100 Hz)보다 **훨씬 높은 출력 주기를 쓸 수 있습니다.**

이전 검토에서 "강체 충돌은 수 ms에 끝나므로 100 Hz로는 피크를 놓칠 수 있다"고 지적했는데,
**이 센서라면 임계값 실험 단계에서 400~500 Hz로 로깅할 수 있습니다.**
그 데이터로 "100 Hz로 낮춰도 피크가 유지되는가"를 검증하면 됩니다.

→ `csv_logger` 와 `safety_monitor` 의 주 루프(현재 100 Hz)를 실험 시에만 올리는 것을 검토.

### 1.4 새로 생긴 문제 3가지

#### ① IMU가 자체 배터리로 동작한다

1셀 리튬폴리머 내장, micro-USB 충전.

- **우리 `battery.py` 는 이 배터리를 못 봅니다.** 메인 팩만 감시합니다.
- **충전 대상이 2개가 됩니다** (메인 팩 + IMU 2개). 시연 전 체크리스트에 반드시 넣을 것.
- IMU 배터리가 방전되면 → 무선 링크 끊김 → **`STALE_IMU_SEC` 워치독이 잡아
  "센서 이상" 부저를 울립니다.** 이 경로는 이미 구현되어 있습니다.
- 실기 통합 시 **IMU를 메인 팩에서 상시 급전**할 수 있는지 확인하세요.
  충전하면서 동작 가능하면 이 문제가 사라집니다.

#### ② 무선 링크 = 새로운 실패 모드

2.4 GHz는 **Wi-Fi·블루투스와 같은 대역**입니다.
시연장처럼 Wi-Fi가 많은 곳에서 간섭으로 패킷이 끊길 수 있습니다.

- 다행히 보행기와 수신기(Pi) 사이 거리는 수십 cm라 **거리는 문제가 아닙니다.**
- **워치독은 이미 대응합니다** — 데이터가 끊기면 값을 무효화하고 소리로 고지.
- 다만 **판정 중간에 끊기면 판정이 진행되지 않습니다**(`imu.valid=False` 면 상태 유지).
  이건 의도된 안전 동작이지만, 실측으로 끊김 빈도를 확인해야 합니다.
- **측정 항목 추가:** 30분 연속 로깅 후 `t_capture` 간격 분포를 보고
  결손 구간의 최대 길이를 확인 → `STALE_IMU_SEC` 를 그 3배로 조정.

#### ③ 수신기 인터페이스를 확인해야 한다

무선 AHRS는 **수신기(리시버)가 별도**입니다.

**확인할 것:**
- 수신기와 Pi 사이가 **USB인가 UART인가**
- USB면 `IMU_PORT` 가 `/dev/ttyUSB*` 또는 `/dev/ttyACM*` → **udev 고정 링크 필요**
- **수신기 하나로 IMU 2개를 받는가**, 아니면 수신기가 2개 필요한가
  (최대 15개 동시 지원이므로 하나로 될 가능성이 높습니다)
- 두 IMU를 구분하는 **센서 ID가 출력에 포함되는가** → 포함된다면
  `sensors/imu_sensor.py` 의 필드 인덱스 가정(`EXPECTED_FIELDS = 9`)이 바뀝니다

> ⚠ **이것이 코드에 직접 영향을 주는 유일한 IMU 변경사항입니다.**
> 출력 포맷이 `<센서ID>, roll, pitch, yaw, gx, gy, gz, ax, ay, az` 형태라면
> `EXPECTED_FIELDS` 를 10으로 바꾸고 인덱스를 +1 해야 합니다.
> **`safety_monitor.py` 기동 시 자동 진단이 "필드 개수 불일치"로 잡아줍니다.**

#### ④ 설정 명령 시퀀스도 재확인

계획서의 `<sof1> <sog1> <soa1> <sor10>` 등은 **유선 EBIMU-9DOF 계열 기준**입니다.
무선 계열은 설정 방법이 다를 수 있습니다(전용 PC 프로그램으로 설정하는 경우가 많음).

→ 제품 매뉴얼에서 확인하고 `sensors/imu_sensor.py` 의 `SETUP_COMMANDS` 를 맞출 것.

### 1.5 부수 효과 — 배선이 훨씬 간단해진다

계획서의 "3.3V UART, TX↔RX 크로스, 2채널 UART 활성화(디버그 콘솔 비활성)"가
**전부 불필요해집니다.** Pi 5 UART 설정 과제가 사라집니다.

또한 **IMU 케이블이 없으므로 진동에 의한 커넥터 이탈 위험도 줄어듭니다.**
보행기 프레임에 붙이는 데는 유리합니다.

---

## 2. GPS: UB-ZED03

### 2.1 코드 영향 — 거의 없을 것으로 예상

`UB-ZED03` 은 **u-blox ZED-F9P 칩을 탑재한 보드**로 보입니다.
칩이 F9P라면 다음이 그대로 동작합니다:

- NMEA 파싱 (`_parse_gga`, `_parse_rmc`)
- 측위 품질 코드 (4 = RTK 고정, 5 = RTK 부동)
- RTCM3 입력을 통한 RTK
- `ntrip_client.py` 전체

### 2.2 확인할 것 4가지

| ☐ | 항목 | 왜 |
| :-- | :--- | :--- |
| ☐ | **인터페이스가 USB인가 UART인가** | USB면 `find_gps_port()` 의 VID 매칭(u-blox `0x1546`)이 동작. **보드에 CP210x/CH340 같은 변환칩이 달려 있으면 VID가 달라집니다** |
| ☐ | **기본 통신속도** | 38400 / 115200 / 9600 중 무엇인지. `GPS_BAUD` 를 맞춰야 함 |
| ☐ | **RTCM 입력 경로** | 우리는 같은 포트로 RTCM을 써 넣습니다(`write_rtcm`). 보드가 이를 F9P의 UART1로 전달하는지 확인 |
| ☐ | **안테나 포함 여부 · 종류** | **RTK는 멀티밴드(L1+L2) 안테나가 필수**입니다. 단주파 안테나면 RTK가 동작하지 않습니다 |

### 2.3 안테나 — **해결됨** (4-Arm Helical 능동 안테나 별도 구매·연결)

계획서 원안대로 **4-Arm Helical(quad helix) 능동 안테나**를 추가 구매해 연결했습니다.
**RTK 하드웨어 쪽 최대 위험이 해소되었습니다.**

쿼드 헬릭스 멀티밴드 안테나는 **ZED-F9P RTK 로버의 표준 조합**입니다.
보드 번들 안테나가 단주파일 위험을 이 구매가 제거했습니다.

#### 헬리컬을 골라서 좋은 점 — 보행기에 특히 유리

| 특성 | 의미 |
| :--- | :--- |
| **그라운드 플레인 불필요** | 패치 안테나는 통상 10×10 cm급 금속 접지면이 필요합니다. 헬리컬은 **필요 없습니다.** 보행기 프레임에 그대로 달 수 있습니다 |
| **가벼움 (수십 g 수준)** | **무게중심 문제와 충돌하지 않습니다** (§ 아래) |
| **축비(axial ratio) 양호** | 다중경로 억제에 유리 — 도심 협곡에서 도움이 됩니다 |

#### 그래도 확인할 것 3가지

| ☐ | 항목 | 왜 |
| :-- | :--- | :--- |
| ☐ | **멀티밴드 표기 (L1 + L2)** | 사양서에 L1/L2 또는 L1/L2/L5가 명시되어야 합니다. **L1 only면 RTK cm급이 나오지 않습니다** |
| ☐ | **바이어스 전압 호환** | 능동 안테나는 수신기에서 전원(통상 3~5 V)을 공급받습니다. **보드 공급 전압과 안테나 요구 전압이 맞는지** 확인. 안 맞으면 LNA가 동작하지 않아 위성이 거의 안 잡힙니다 |
| ☐ | **커넥터 규격** | SMA / MCX / u.FL. 변환 어댑터가 필요한지, 케이블 손실이 큰지 |

> **증상으로 판별하는 법:** 실외에서 위성 수(`satellites`)가 5개 미만이거나
> HDOP이 계속 5 이상이면 바이어스 전압이나 커넥터를 의심하세요.
> 대시보드와 보호자 알림에 위성 수·HDOP이 항상 표시되므로 바로 보입니다.

#### 새로 생긴 기구 설계 항목 — 장착 위치

안테나는 **하늘이 보여야** 합니다. 그런데 보행기에서는 **사용자의 몸통이 하늘의 절반을 가립니다.**

- **손잡이보다 높게**, 사용자 앞쪽 또는 측면에 세워 다는 것이 좋습니다
- 헬리컬은 **수직 원통형이므로 위(천정)를 향해 세워야** 합니다.
  패치처럼 눕혀 달면 안 됩니다
- 케이블 라우팅 — 프레임을 따라 F9P 보드까지. **진동 이탈 방지 고정** 필요

> **무게중심과의 관계:** 앞서 "상단에 무거운 것을 얹으면 전도 위험이 커진다"고 지적했는데,
> **헬리컬 안테나는 수십 g에 불과해 이 문제와 충돌하지 않습니다.**
> 높이 다는 것이 정답입니다. 무거운 것(배터리·보드)만 최하단에 두면 됩니다.

#### RTK에 남은 블로커는 이제 둘뿐

| 항목 | 상태 |
| :--- | :--- |
| ~~멀티밴드 안테나~~ | ✅ **해결** |
| ~~F9P 수신기~~ | ✅ 보유 (UB-ZED03) |
| ~~NTRIP 클라이언트 코드~~ | ✅ 구현 완료 (`ntrip_client.py`) |
| **LTE 회선 + 데이터 SIM** | ❌ **미조달** |
| **NTRIP 캐스터 계정** | ❌ 미신청 (국토지리정보원 등) |

→ **이 둘만 해결되면 "cm 단위 정밀 위치"가 실제로 성립합니다.**
   계정 신청은 하드웨어 없이 지금 할 수 있습니다.

---

## 3. LiDAR: 시그봇 **D2** — 계획서와 동일 (확인 완료)

`C1` 은 착오였고 실제로는 **CygLiDAR D2**, 즉 계획서 원안 그대로입니다.
따라서 3D 점군·단차 감지·듀얼 구성 계획이 **전부 그대로 유효합니다.**

### 3.1 확정 사양

| 항목 | 2D 모드 | 3D 모드 |
| :--- | :--- | :--- |
| 유효 거리 | **0.2 m ~ 7.0 m** | **0.05 m ~ 2.0 m** |
| 수평 화각 | 120° | 120° |
| 수직 화각 | — | 65° |
| 해상도 | 1° (각도) | 160 × 60 px (= 9,600점) |
| 측정 속도 | 15 Hz | 15 Hz |
| 거리 정확도 | ±1% | ±1% |
| 인터페이스 | USB / 3.3 V UART | 동일 |
| 크기·무게 | 37×37×24 mm, 28 g | 동일 |

**2D와 3D를 동시에 얻을 수 있습니다.**

### 3.2 ⚠ 이 사양이 드러낸 설정 오류 2건

기존 `config.py` 의 유효 거리 설정이 **두 모드 중 어느 쪽과도 맞지 않았습니다.**

| 설정 | 기존값 | 문제 | 수정 |
| :--- | ---: | :--- | :--- |
| `LIDAR_MIN_VALID_M` | 0.10 | **2D 최소 거리는 0.2 m** — 0.1~0.2 m의 쓰레기값을 유효로 받아들임 | 모드별 분리 |
| `LIDAR_MAX_VALID_M` | 8.00 | **3D 최대는 2.0 m, 2D도 7.0 m** — 존재할 수 없는 거리를 유효로 받아들임 | 모드별 분리 |

→ `config.py` 에 `LIDAR_MODE` 를 추가하고 모드별 유효 범위를 분리했습니다.

### 3.3 3D 모드의 2 m 제한 — 경고 거리와 맞는가

**맞습니다. 여유도 있습니다.**

```
유도된 정지 거리                    ≈ 0.51 m
DIST_WARN_M (정지거리 ×2)           = 1.00 m
상향 검토안 (정지거리 ×2.5~3)       = 1.2 ~ 1.5 m
CURB_WARN_M (단차 경고)             = 1.20 m
────────────────────────────────────────────
3D 유효 최대                        = 2.00 m   ← 전부 이 안에 들어옴
```

→ **3D 모드만으로도 장애물 경고와 단차 감지가 모두 성립합니다.**

### 3.4 권장 운용 — 2D와 3D를 함께

| 모드 | 용도 | 근거 |
| :--- | :--- | :--- |
| **3D** | 근거리 장애물 경고 + **단차(턱) 감지** | z 정보가 있어야 지면 평면 추정이 가능. 2 m로 충분 |
| **2D** | 원거리 접근 인지 (선택) | 7 m — 멀리서 다가오는 장애물의 조기 인지 |

기획 배경의 **"턱 걸림"이 살아납니다.** `jetson/lidar_bridge_node.py` 의
단차 감지 코드(지면 평면 추정 → z 편차)가 그대로 쓰입니다.
**단, 3D `PointCloud2` 모드여야 합니다** — 2D `LaserScan` 에는 z가 없습니다.

### 3.5 여전히 유효한 우려 3가지

#### ① 실외 태양광 — 3D 모드에서 더 심각할 수 있다

솔리드스테이트 ToF의 직사광 취약성은 그대로입니다.
**3D 모드의 유효거리가 2 m뿐이라 여유가 작습니다.**
햇빛 아래에서 1.5 m로 줄어들면 경고 거리(1.0~1.5 m)와 겹쳐 버립니다.

→ **§2-3 실외 실측 시 반드시 2D·3D 모드를 각각 측정하세요.**
   3D가 실외에서 못 쓸 정도면 단차 감지를 포기하고 2D만 쓰는 판단이 필요합니다.

#### ② 듀얼 구성의 상호 간섭

같은 파장의 ToF 2대가 화각을 겹치면 상대 조명광을 자기 반사광으로 오인할 수 있습니다.
화각을 물리적으로 분리하고, 겹침 영역 오차를 실측해 기록하세요.

#### ③ 프레임 포맷은 여전히 미검증

`sensors/lidar_serial.py` 의 payload 오프셋·비트폭은 **추정값**입니다.
D2가 확정되었으므로 `tools/lidar_probe.py` 가 그대로 유효합니다 (헤더 `5A 77 FF`).

```bash
python tools/lidar_probe.py dump --port COM3 --seconds 5 --truth 1.00
python tools/lidar_probe.py analyze logs/lidar_raw_COM3.bin --truth 1.00
```

**3D 모드는 160×60 = 9,600점입니다.** 프레임당 페이로드 크기로 12비트 패킹 여부를
역산할 수 있습니다 — 9,600점 × 2바이트 = 19,200 B vs 12비트 패킹 = 14,400 B.
`lidar_probe.py analyze` 가 출력하는 페이로드 길이를 보면 바로 알 수 있습니다.

### 3.6 Jetson의 역할 — 유지 타당

3D 점군이 초당 9,600 × 15 × 2대 = **약 29만 점**입니다.
numpy로 처리하면 Pi도 가능하지만, 단차 감지의 평면 추정까지 얹으면
**Jetson에 두는 것이 여유롭습니다.** 계획서 원안대로 유지하세요.

"AI 비전"은 여전히 카메라가 없으므로 문구를 삭제하거나 카메라를 추가해야 합니다.

## 4. 확인 완료

LiDAR는 **CygLiDAR D2** 로 확정되었습니다 (계획서 원안과 동일).
따라서 §3의 초기 우려(2D 전용 가정)는 해소되었고, 단차 감지도 살아 있습니다.

---

## 5. 지금 바로 반영할 수 있는 것 (LiDAR 답변과 무관)

| ☐ | 작업 |
| :-- | :--- |
| ☐ | IMU **가속도 ±16 g, 자이로 2000 dps** 설정 |
| ☐ | IMU 수신기 인터페이스 확인 → `IMU_PORT` 수정 |
| ☐ | IMU 출력 포맷 확인 → `EXPECTED_FIELDS` 조정 (센서 ID 포함 여부) |
| ☐ | IMU 배터리 충전 절차를 시연 체크리스트에 추가 |
| ☐ | 2.4 GHz 링크 결손 구간 30분 측정 → `STALE_IMU_SEC` 조정 |
| ☐ | GPS 보드 인터페이스·통신속도 확인 |
| ☐ | 안테나 **멀티밴드(L1/L2) 표기 · 바이어스 전압 · 커넥터** 확인 |
| ☐ | 안테나 장착 위치 결정 — 손잡이보다 높게, 수직으로 세워서 |
| ☐ | **NTRIP 캐스터 계정 신청** (하드웨어 없이 지금 가능) |
| ☐ | 계획서 §2.2 BOM 표를 실제 부품으로 수정 |
| ☐ | **`LIDAR_MODE` 확인** — 3D(단차 감지 가능, 2 m) / 2D(7 m, z 없음) |
| ☐ | 실외 실측 시 **2D·3D 모드를 각각** 측정 |
| ☐ | 계획서 §2.1의 "AI 비전 처리" — 카메라가 없으므로 문구 삭제 또는 카메라 추가 |

---

## 6. 종합 — 좋아진 것과 나빠진 것

### 좋아진 것

- **IMU 배선 과제 소멸** — 3.3V UART, 2채널 활성화, 디버그 콘솔 비활성이 전부 불필요
- **높은 출력 주기 가능** (내부 1000 Hz) → 충격 피크 포착에 유리
- **케이블 이탈 위험 감소** — 진동 환경에 유리
- **LiDAR는 계획서 원안 그대로** — 3D 점군·단차 감지·듀얼 구성 계획이 전부 유효

### 나빠진 것

- **IMU 배터리 관리 부담** — 충전 대상 3개(메인 팩 + IMU 2개), 독립 방전
- **2.4 GHz 간섭** — 새로운 실패 모드
- **3D 유효거리가 2 m뿐** — 실외에서 줄어들면 경고 거리와 겹칠 위험 (§3.5)

### 바뀌지 않은 것

- **FSR 설정 확인은 여전히 최우선** (2~16 g 설정 가능이므로)
- **LTE 회선 필요** — RTK와 알림 둘 다
- 실외 태양광 실측 필요
- 무게중심 · 전원 · 실험 데이터 · 보고서 챕터

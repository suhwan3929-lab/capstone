# 코드 리뷰 — capstone_code

> **대상:** `capstone_code/` (main_controller.py 757줄, sensors/ 3개 모듈 621줄)
> **검토일:** 2026-08-20
> **관련 문서:** [계획_검토_및_보완사항.md](계획_검토_및_보완사항.md)

---

## 0. 총평

**동작하는 프로토타입이고, 구조 감각이 좋다.** 특히 다음은 잘 되어 있다.

- 센서 모듈이 `get_snapshot()` / `start()` / `stop()` 로 **깔끔하게 분리**되어 있다.
  → GUI가 센서 내부를 모른다. 이 구조 덕분에 나중에 ROS 2로 옮기는 작업이 **기계적으로** 가능하다. (§7)
- `try: import ... except ImportError: x = None` 으로 **센서가 없어도 GUI가 뜬다.** 개발 편의성이 좋다.
- `_lock` 으로 스레드 간 공유 데이터를 보호하는 습관이 들어 있다.
- IMU에 **FSM(상태 머신)** 을 도입해 "충격 즉시 신고"를 피하고 사용자 확인 팝업까지 넣었다.
  이전 계획서 리뷰에서 지적했던 **취소 경로가 이미 구현되어 있다.** 큰 진전이다.

**다만 지금 상태로는 안전 시스템으로 신뢰할 수 없다.** 이유는 세 가지다.

| # | 문제 | 결과 |
| :-- | :--- | :--- |
| 1 | **센서 주도 낙상 경로가 문자열 불일치로 끊겨 있다** (§1.1) | GUI 팝업 없이 낙상이 확정되면 **신고가 나가지 않는다** |
| 2 | **세 모듈 모두 런타임 재연결이 없고, 멈춰도 "정상"으로 표시된다** (§1.2, §1.3) | 센서가 죽어도 **아무도 모른다.** 화면은 마지막 값을 계속 보여준다 |
| 3 | **낙상 판정에 "쓰러진 자세" 조건이 없다** (§1.5) | 보행기를 바닥에 내려놓고 자리를 뜨면 **보호자에게 사고 신고가 간다** |

아래는 심각도 순이다. **Critical 6건은 시연 전에 반드시 잡아야 한다.**

---

## 1. Critical — 기능이 동작하지 않거나 안전을 훼손

### 1.1 [C1] 낙상 상태 문자열 불일치 — 신고가 나가지 않는 경로가 있다

```python
# sensors/imu_sensor.py:42
STATE_FALLEN = "낙상 확정"
```
```python
# main_controller.py:715
if display_fall_state == "사고 확정" and not self._alert_sent:
    self._send_emergency_report(...)
```

**`"낙상 확정"` 과 `"사고 확정"` 은 다른 문자열이다.**

- IMU FSM이 **자체 10초 타임아웃**(`imu_sensor.py:203`)으로 `STATE_FALLEN`에 도달하면
  → `raw_fall_state = "낙상 확정"` → `display_fall_state = "낙상 확정"`
  → **`== "사고 확정"` 이 거짓 → 디스코드 신고가 발송되지 않는다.**
- 게다가 `IMUCard.state_map`(`main_controller.py:269`)에 **`"낙상 확정"` 키가 없다.**
  `state_map.get(...)` 이 `None`을 반환하고 `if cfg:` 가 거짓이 되어
  **IMU 카드가 이전 상태("응답 대기")를 그대로 표시한 채 멈춘다.**

현재 신고가 나가는 유일한 경로는 GUI 팝업에서 사용자가 "예"를 누르거나 팝업 타이머가 끝나
`self._force_accident = True`(`main_controller.py:622`)가 되는 경우뿐이다.
**팝업이 뜨지 않는 상황(이미 다른 팝업이 열려 있거나, `require_user_check` 전달 타이밍이 어긋난 경우)에서는
낙상이 확정되어도 아무 일도 일어나지 않는다.**

또한 `state_map`에는 IMU 모듈에 **존재하지 않는 상태 2개**(`"사고 확정"`, `"신고완료-정상"`)가 들어 있고,
**실제 종착 상태(`"낙상 확정"`)는 빠져 있다.** 두 모듈이 따로 수정되며 어긋난 흔적이다.

**수정 방향 — 문자열을 공유 상수로:**

```python
# sensors/states.py (신규)
STATE_NORMAL      = "평상시"
STATE_STABILIZING = "충격 감지"
STATE_CHECKING    = "움직임 분석중"
STATE_ASKING      = "응답 대기"
STATE_FALLEN      = "낙상 확정"
```

`main_controller.py`와 `imu_sensor.py`가 **같은 모듈에서 import** 하도록 바꾸면
이 부류의 버그가 구조적으로 불가능해진다. 지금처럼 문자열 리터럴을 양쪽에 손으로 적으면
오타 하나가 조용히 신고를 막는다. **안전 시스템에서 가장 위험한 실패 형태다.**

### 1.2 [C2] 런타임 재연결이 없다 — 한 번 끊기면 세션 내내 죽어 있다

```python
# sensors/imu_sensor.py:222
        except Exception:
            break   # 통신 에러 발생 시 루프 탈출
```

세 모듈 모두 같다.

| 모듈 | 위치 | 동작 |
| :--- | :--- | :--- |
| IMU | `imu_sensor.py:222` | 예외 1회 → `break` → 스레드 종료, 재시도 없음 |
| GPS | `gps_sensor.py:207` | 예외 → status ERROR, 재시도 없음 |
| LiDAR | `lidar_sensor.py:145` | 예외 → status ERROR, 재시도 없음 |

시작 시에는 3회 재시도 로직을 잘 넣어 두었는데(`run_integrity_check`),
**정작 운용 중 끊김에는 대응이 전혀 없다.** USB 순간 접촉 불량, 전압 강하로 인한 재열거,
디코드 오류 한 번이면 **그 센서는 프로그램을 재시작할 때까지 영구히 죽는다.**

보행기는 진동 환경이다. 이 상황은 "혹시"가 아니라 "언제"의 문제다.

**수정 방향 — 감시 루프로 감싸기:**

```python
def _imu_thread():
    backoff = 1.0
    while imu_data.get("running"):
        ser = None
        try:
            ser = serial.Serial(SERIAL_PORT, BAUD_RATE, timeout=1)
            _setup_sensor(ser)
            update("status", ST_OK)
            backoff = 1.0                      # 연결 성공 시 백오프 리셋
            _read_loop(ser)                    # 기존 while 루프를 함수로 분리
        except Exception as e:
            update("status", ST_ERROR)
            _log(f"IMU 연결 끊김: {e} — {backoff:.0f}초 후 재연결")
        finally:
            try:
                if ser: ser.close()
            except Exception: pass
        if not imu_data.get("running"): break
        time.sleep(backoff)
        backoff = min(backoff * 2, 30.0)       # 지수 백오프, 상한 30초
```

### 1.3 [C3] 무응답 감지가 없다 — 센서가 멈춰도 화면은 "정상"

```python
# sensors/imu_sensor.py:151
update("status", ST_OK)   # 파싱 성공 시에만 갱신
```

`ST_OK`는 **한 번 설정되면 스스로 내려가지 않는다.**

- IMU: 센서가 전원은 살아 있는데 데이터를 안 보내면 → `ser.readline()`이 빈 값 반환 →
  `len(parsed) < 9` → `continue`. **예외가 나지 않으므로 `break`도 안 걸리고, status는 `"정상"` 유지.**
  `roll`/`pitch`/`accel_mag`는 **마지막 값에 얼어붙은 채 화면에 계속 표시된다.**
- LiDAR: `_parse_frame`이 계속 False를 반환해도 status는 마지막 `ST_OK` 그대로.
  **`min_dist`가 프리즈되어, 실제로는 앞이 비었는데 "0.4 m 위험"으로 화면이 계속 깜박이거나,
  반대로 실제 장애물이 있는데 "2.5 m OK"로 표시된다.**
- GPS: `_parse_gga` 성공 시에만 `ST_OK`. 신호가 끊겨도 status는 정상 유지.

**이것이 계획서 리뷰에서 말한 "조용히 죽는 것(silent failure)"의 실제 사례다.**
안전 시스템에서 가장 나쁜 실패는 고장이 아니라 **고장을 정상으로 보고하는 것**이다.

**수정 방향 — 수신 시각 기록 + GUI 워치독:**

```python
# 각 센서 모듈: 유효 데이터 수신 시마다
update("last_rx", time.time())

# main_controller._refresh() 안에서
STALE_SEC = {"imu": 0.5, "lidar": 1.0, "gps": 3.0}   # 센서별 허용 지연
def _is_stale(snap, kind):
    t = snap.get("last_rx")
    return t is None or (time.time() - t) > STALE_SEC[kind]

if _is_stale(snap_imu, "imu"):
    i_stat = "연결 실패"          # 카드가 즉시 빨간색으로 전환
    self._sys_log("⚠ IMU 응답 없음 — 데이터 갱신 중단", "ERROR")
```

**추가로: 낙상 감지가 죽은 상태를 사용자에게 반드시 알려야 한다.** 화면 표시만으로는 부족하고,
부저로 주기적인 "센서 이상" 신호를 내보내야 한다.

### 1.4 [C4] LiDAR 프레임 파싱 — 검증 없이 신뢰하고 있다

`sensors/lidar_sensor.py:68-104`에 문제가 4개 겹쳐 있다.

**① 체크섬을 읽고 버린다**
```python
cs_byte = ser.read(1)              # lidar_sensor.py:85
if len(payload) != plen or not cs_byte: return False
```
길이만 확인하고 **체크섬 값 자체를 검증하지 않는다.** 프로토콜이 체크섬을 제공하는데 쓰지 않는 것은
가장 값싼 방어를 포기하는 것이다. 깨진 프레임이 그대로 거리값이 되어 **허위 위험 경보**를 만든다.

**② 헤더 재동기화 버그**
```python
if b[0] == 0x5A:
    b2 = ser.read(1)
    if b2 and b2[0] == 0x77: ...
```
바이트열이 `5A 5A 77 FF` 로 들어오면: 첫 `5A` 확인 → 다음 바이트가 `5A`(≠`0x77`) → 실패 →
**이미 소비한 `5A`로 되돌아가지 못하고 `77`부터 다시 찾는다.** 페이로드 안에 `0x5A`가 섞이면
동기가 어긋난 채로 진행된다. **롤링 버퍼 + `bytes.find()` 방식으로 바꿔야 한다.**

**③ `payload[5:]`, 2바이트/포인트 가정에 근거가 없다**
```python
dist_data = payload[5:]            # 왜 5인지 주석 없음
num_pts   = len(dist_data) // 2    # 2바이트/포인트 가정
dist_mm   = dist_data[idx] | (dist_data[idx+1] << 8)
```
**CygLiDAR의 3D 모드는 통상 거리값을 12비트로 패킹한다**(2포인트 = 3바이트).
그렇다면 위 파싱은 **완전히 틀린 값**을 만들어내는데, 결과가 "그럴듯한 숫자"라서 **틀린 줄 모른다.**
`_worker`가 보내는 모드 설정 명령(`lidar_sensor.py:135`의 `0x5A 0x77 0xFF 0x02 0x00 0x01 0x00 0x03`)이
2D인지 3D인지에 따라 포맷이 달라진다.

→ **데이터시트로 반드시 확인할 것.** 확인 전까지 이 코드의 거리값은 신뢰할 수 없다.
→ 확인 후 **오프셋과 비트 폭을 상수로 빼고 주석에 데이터시트 페이지를 명시**하라.
→ **검증 방법:** 줄자로 잰 0.5 m / 1.0 m / 2.0 m 지점에 판을 세우고 출력값을 비교. 10분이면 판별된다.

**④ 1바이트씩 2048회 읽기**
```python
for _ in range(2048):
    b = ser.read(1)
```
3 Mbps 링크에서 pyserial의 호출당 오버헤드를 생각하면 **처리량을 따라갈 수 없다.**
블록 단위(`ser.read(ser.in_waiting or 1)`)로 읽어 버퍼에 누적하고 버퍼에서 헤더를 찾는 구조로 바꿔야 한다.

### 1.5 [C5] 낙상 판정에 "쓰러진 자세" 조건이 없다

모듈 docstring은 **"쓰러짐 자세 유지 감지 알고리즘 적용"** 이라고 적혀 있는데,
실제 FSM(`imu_sensor.py:167-217`)은 이렇게 동작한다:

```
충격 2.8g 이상  →  5초 대기  →  5초간 움직임 관찰  →  안 움직이면 사고
```

**어디에도 `roll`/`pitch`를 보는 조건이 없다.** `roll`, `pitch`는 계산해서 화면에는 띄우지만
**판정에는 STATE_CHECKING의 "변화량"으로만 쓰이고, "지금 누워 있는가"는 한 번도 묻지 않는다.**

결과:

| 상황 | 현재 코드의 판정 |
| :--- | :--- |
| 보행기를 바닥에 툭 내려놓고 자리를 뜸 | **사고 확정 → 보호자에게 신고** |
| 차 트렁크에 싣고 문을 닫음 | **사고 확정 → 보호자에게 신고** |
| 턱을 넘은 뒤 신호등에서 5초 이상 정지 | **사고 확정 → 보호자에게 신고** |

세 번째가 특히 위험하다. **횡단보도 대기는 고령자 보행에서 가장 흔한 행동**이다.

**수정 — STATE_CHECKING 진입/확정 조건에 자세를 추가:**

```python
TIPOVER_TILT_DEG = 60.0   # 이 각도 이상 기울어야 "쓰러진 것"

elif current_state == STATE_STABILIZING:
    if (now - impact_time) >= STABILIZE_TIME:
        tilt = max(abs(roll), abs(pitch))
        if tilt < TIPOVER_TILT_DEG:
            current_state = STATE_NORMAL     # 충격은 있었지만 서 있음 → 방지턱/내려놓기
            continue
        current_state  = STATE_CHECKING
        ...
```

이 한 조건만 추가해도 위 표의 오탐 대부분이 사라진다.
(**주의:** IMU 장착 각도가 0°가 아니면 기준을 보정해야 한다. §3.4)

### 1.6 [C6] IMU 필드 인덱스가 조용히 밀릴 수 있다

```python
# sensors/imu_sensor.py:148, 154-156
if len(parsed) < 9: continue
roll, pitch, yaw = parsed[0:3]
gx, gy, gz       = parsed[3:6]
ax, ay, az       = parsed[6:9]
```

- 조건이 **`< 9`(이하 배제)** 이므로, 센서가 **10개 이상 필드를 내보내면 그대로 통과**한다.
  출력 항목이 하나라도 추가되면(온도, 지자기, 타임스탬프 등) **인덱스가 밀려
  자이로 값을 가속도로 읽게 되는데, 프로그램은 아무 오류도 내지 않는다.**
- `_setup_sensor`(`imu_sensor.py:80`)가 보내는 `<sof1><sog1><soa1><som0><sod0><sot0><sots0>`의
  출력 순서가 코드의 가정과 일치하는지 **검증되지 않았다.** 명령 전송 순서와 스트림 필드 순서는 별개다.
- 게다가 **명령에 대한 응답(ack)을 확인하지 않는다.** `ser.read_all()` 결과를 버린다.
  설정이 실패해도 성공한 것처럼 진행된다.

**수정:** `if len(parsed) != 9: continue` 로 바꾸고, 시작 시 첫 N개 샘플로
**정지 상태 `accel_mag ≈ 1.0 ± 0.1` 인지 자동 검증**하라. 아니면 필드 순서가 틀린 것이다.
이 자체 진단 한 줄이 위 문제를 전부 잡는다.

```python
# 시작 직후 자동 검증
if abs(accel_mag - 1.0) > 0.15 and sample_count < 50:
    log("⚠ 정지 상태 가속도가 1g가 아님 — 필드 순서/단위/FSR 설정 확인 필요", "ERROR")
```

> **연계:** 계획서 리뷰 §3.2에서 지적한 **가속도계 측정범위(FSR)** 문제가 그대로 살아 있다.
> `IMPACT_THRESHOLD = 2.8`인데 FSR이 ±2 g면 **이 임계값에 영원히 도달하지 못한다.**
> 위 자체 진단과 함께 반드시 확인할 것.

---

## 2. High — 오작동하거나 골든타임을 갉아먹는다

### 2.1 [H1] Webhook URL 하드코딩 + 가드가 무력화되어 있다

```python
# main_controller.py:23
DISCORD_WEBHOOK_URL = "https://discord.com/api/webhooks/..."
```
```python
# main_controller.py:548
if not DISCORD_WEBHOOK_URL:      # 플레이스홀더도 빈 문자열이 아니므로 항상 거짓
    self._sys_log("웹훅 URL이 설정되지 않았습니다.", "ERROR")
    return
```

**가드가 절대 발동하지 않는다.** 미설정 상태로 사고가 나면 "URL 미설정" 경고 대신
**잘못된 URL로 POST를 시도하고 실패 로그만 남긴다.** 원인 파악이 어려워진다.

그리고 이 자리가 바로 **실제 토큰이 소스에 박힌 채 공유되는** 지점이다.
(계획서 리뷰 §6 참조 — 유출 시 제3자가 고령자 실시간 위치를 수신할 수 있다.)

```python
import os
DISCORD_WEBHOOK_URL = os.environ.get("WALKER_WEBHOOK_URL", "").strip()

def _is_valid_webhook(u: str) -> bool:
    return u.startswith("https://discord.com/api/webhooks/") and len(u) > 45
```

### 2.2 [H2] 신고 전에 지도 이미지를 먼저 받는다 — 최대 10초 손해

```python
# main_controller.py:581-591
def _task():
    image_bytes = self._fetch_static_map(lat, lon)   # timeout=10
    self._send_webhook_task(payload, image_bytes)     # 그 다음에야 전송
```

**지도 그림은 있으면 좋은 것이고, 신고는 즉시 가야 하는 것이다.** 순서가 반대다.
LTE가 불안정한 실외에서 지도 서버가 느리면 **10초를 통째로 잃는다.**

전체 지연을 계산해 보면:

| 구간 | 시간 |
| :--- | :--- |
| 충격 → STABILIZE (`STABILIZE_TIME=5.0`) | 5초 |
| → CHECKING (`CHECK_TIME=5.0`) | 5초 |
| → 사용자 응답 대기 (팝업 10초) | 10초 |
| → 지도 이미지 다운로드 | 최대 10초 |
| → 웹훅 전송 | 최대 5~10초 |
| **합계 (최악)** | **최대 35초** |

프로젝트 목적이 **"골든타임 확보"** 인데, 판정 로직만으로 20초를 쓰고 있다.

**수정 — 2단 전송:**

1. **충격 감지 즉시(STABILIZING 진입 시점)** 위치를 포함한 **예비 알림**을 보낸다.
   → 보호자가 그 시점부터 이동을 시작할 수 있다.
2. 판정이 끝나면 **확정 또는 취소 후속 알림**을 보낸다.
3. 지도 이미지는 **텍스트 알림 발송 이후 별도로** 첨부한다.

추가로 `staticmap.openstreetmap.de`는 **제3자 무료 서비스**다. 가용성 보장이 없고,
**사용자의 좌표를 알림 수신자가 아닌 외부 서버에 한 번 더 노출**시킨다는 점도 고려하라.

### 2.3 [H3] `min_dist` = 프레임 전체 최솟값 — 픽셀 하나가 오경보를 만든다

```python
# sensors/lidar_sensor.py:92-99
for i in range(num_pts):
    if 0 < dist_mm < 16000:
        if dist_m < min_d: min_d = dist_m
```

문제가 셋이다.

1. **단일 최솟값**을 쓴다. ToF는 반사·난반사·태양광으로 **튀는 픽셀이 반드시 생긴다.**
   나쁜 픽셀 하나가 `0.03 m`를 내면 → 즉시 `위험` → 화면 전체 깜박임. **시연 중 오경보 1순위 원인이다.**
   → **하위 5퍼센타일** 또는 **N개 이상 연속 포인트가 임계 이내일 때만** 인정하도록 바꿀 것.
   → 추가로 **연속 프레임 확인**(2~3 프레임 연속 위험일 때만 경보)으로 단발 노이즈를 제거.
2. **최소 거리 하한이 없다.** `0 < dist_mm` 이므로 1 mm도 유효로 본다.
   ToF는 최소 측정거리 미만에서 쓰레기값을 낸다. **데이터시트 최소거리 미만은 버릴 것.**
3. **16000 mm(16 m) 상한은 D2 사양을 넘는다.** 유효 범위 밖 값을 걸러내지 못한다.

### 2.4 [H3-b] 관심영역(ROI)이 없다 — 바닥이 장애물로 잡힌다

`angles` 필드는 딕셔너리에 선언만 되어 있고(`lidar_sensor.py:38`) **한 번도 채워지지 않는다.**
즉 **"얼마나 가까운가"만 알고 "어디에 있는가"는 모른다.**

센서를 조금이라도 아래로 기울이면 **전방 바닥면이 화각에 들어와 0.5 m 장애물로 잡힌다.**
보행기 자체 프레임·손잡이가 시야에 걸려도 마찬가지다.

→ 각도 데이터를 파싱해 저장하고, **각도 창(예: 정면 ±40°) + 높이 필터**를 적용해야 한다.
→ 이것은 계획서의 "턱 감지" 기능으로 가는 전제이기도 하다.

### 2.5 [H4] 화면 깜박임과 낙상 팝업이 서로를 방해한다

`FlashOverlay`(`main_controller.py:43`)는 `overrideredirect(True)` + `-topmost` 로
**루트 창 전체를 덮는다.** 낙상 팝업(`main_controller.py:600`)도 `-topmost` 다.

**두 topmost 창이 겹치면 z-order가 보장되지 않는다.**

그리고 **이 둘이 동시에 뜨는 상황이 가장 흔하다.** 장애물에 부딪혀 넘어지면
LiDAR 위험(깜박임)과 IMU 충격(팝업)이 **같이** 발생한다.
그 순간 **사용자가 "아니오(취소)" 버튼을 못 누르면 오신고가 그대로 나간다.**

→ **팝업이 열리는 동안 오버레이를 강제 중지**하고, 팝업이 닫히면 재개하라.

```python
def _show_fall_popup(self):
    if self._flash_danger_active:
        self.flash_overlay.stop()      # 팝업 우선
    ...
```

### 2.6 [H5] 배터리 표시가 항상 100%다 — 거짓 정보

```python
# main_controller.py:471
if os.path.exists("/sys/class/power_supply/BAT0/capacity"):
```

**Raspberry Pi 5에는 `BAT0`가 없다.** 자작 리튬팩은 `/sys/class/power_supply`에 나타나지 않는다.
따라서 이 함수는 **항상 `(100.0, False)`를 반환**하고, 화면에는 늘 초록색 100%가 뜬다.

**틀린 계기판은 없는 계기판보다 나쁘다.** 배터리가 5% 남았는데 100%로 표시되는 상태에서
시연 도중 전원이 나가면 원인 파악조차 못 한다.

→ **I²C 연료 게이지(INA219 / MAX17043 등) 또는 ADC로 실제 전압을 읽어라.**
→ 그전까지는 **위젯을 회색 "측정 안 됨"으로 표시**하라. 100%를 보여주면 안 된다.
→ 계획서 리뷰 §5.5의 **잔량 5% 자동 안전 종료**도 여기에 연결된다.

### 2.7 [H6] 움직임 2회로 실제 낙상이 취소된다 — 미탐 위험

```python
# sensors/imu_sensor.py:189-194
if d_accel > 1.2 or d_roll > 25 or d_pitch > 25 or gyro_mag > 80:
    move_count += 1
    settled_accel, settled_roll, settled_pitch = accel_mag, roll, pitch
if move_count >= REQUIRED_MOVE_COUNT:      # == 2
    current_state = STATE_NORMAL
```

100 Hz로 들어오는 데이터에서 **연속된 두 샘플이 노이즈로 튀기만 해도 `move_count`가 2가 되어
낙상 판정이 취소된다.** 시간 간격 조건이 전혀 없다.

**오탐(false positive)보다 미탐(false negative)이 훨씬 위험하다.** 실제로 쓰러진 사람이
경련하거나 몸을 뒤척였다는 이유로 "안전"으로 분류되면 신고가 나가지 않는다.

→ **움직임 이벤트 간 최소 시간 간격(예: 0.5초 이상)** 을 요구하고,
  단일 샘플 비교 대신 **구간 이동평균/모션 에너지 적분**을 쓸 것.
→ `settled_*` 기준점을 **한 샘플로 캡처**(`imu_sensor.py:179`)하는 것도 같은 문제다.
  노이즈 낀 한 샘플이 5초 관찰 구간 전체의 기준이 된다. **0.5초 평균**으로 잡아라.

### 2.8 [H7] GPS 신호를 잃어도 마지막 좌표가 남는다

```python
# sensors/gps_sensor.py:107-117
if lat_raw and len(lat_raw) >= 7:      # 값이 있을 때만 갱신
    update("lat", ...)
```

fix를 잃으면 GGA의 위경도 필드가 **비어서** 들어온다. 그러면 `update`가 호출되지 않고
**`lat`/`lon`은 마지막 값을 그대로 유지한다.** `fix_quality`만 0이 된다.

→ 화면과 신고 메시지는 **몇 분 전 좌표를 현재 위치인 것처럼** 보여준다.

게다가 신고 메시지(`main_controller.py:564`)에는 **`fix_quality` / `satellites` / `hdop`가
하나도 들어가지 않는다.** GPS 모듈이 이미 다 파싱해 두었는데(`gps_sensor.py:119-121`) 쓰지 않는다.

**보호자 입장에서 "정확도 ±2 cm"와 "정확도 ±5 m"는 완전히 다른 정보다.**

```python
# 신고 메시지에 추가
FIX_NAME = {0: "측위 불가", 1: "단독측위", 2: "DGPS", 4: "RTK 고정", 5: "RTK 부동"}
{"name": "측위 품질", "value": f"{FIX_NAME.get(q, q)} / 위성 {sats}개 / HDOP {hdop}", "inline": True},
{"name": "좌표 신선도", "value": f"{age:.0f}초 전", "inline": True},
```

`fix_quality == 4`(RTK Fixed)를 표시하는 것은 **계획서가 내세운 "cm 단위 정밀 위치"를
실제로 증명하는 유일한 근거**이기도 하다. 반드시 넣어라.

### 2.9 [H8] "포트가 열렸다" ≠ "센서가 살아 있다"

```python
# sensors/gps_sensor.py:161-164
with serial.Serial(port, GPS_BAUDRATE, timeout=1):
    pass
success = True
log("✅ 포트 열기 성공", "INFO")
```

`lidar_sensor.py:120-122` 도 같다. **포트 열기만 성공하면 "연결 성공"으로 로그를 찍는다.**
케이블이 반쯤 빠져 있거나 센서 전원이 없어도 **USB-시리얼 변환칩만 살아 있으면 성공으로 뜬다.**

→ 무결성 검사는 **일정 시간(예: 2초) 안에 유효 데이터 1건 이상 수신**을 조건으로 해야 한다.
  GPS는 유효 NMEA 문장 1개, IMU는 필드 9개짜리 샘플 1개, LiDAR는 유효 프레임 1개.

---

## 3. Medium — 구조·운용 문제

### 3.1 [M1] 스레드에서 tkinter를 건드리고 있다

```python
# main_controller.py:482
def _sys_log(self, msg, level="INFO"):
    self.root.after(0, lambda: self.terminal.append_log(msg, level))
```

`root.after()`를 워커 스레드에서 호출하는 것은 **널리 쓰이지만 공식적으로 보장되지 않는다.**
tkinter는 스레드 안전하지 않으며, 부하가 걸리면 `RuntimeError: main thread is not in main loop`가
간헐적으로 발생한다. **재현이 어려워 디버깅에 시간을 많이 뺏기는 종류의 문제다.**

이미 100 ms 갱신 루프가 있으니, **`queue.Queue`에 로그를 넣고 `_refresh()`에서 비우는 방식**으로
바꾸면 확실하고 코드도 더 짧아진다.

```python
self.log_q = queue.Queue()
def _sys_log(self, msg, level="INFO"):
    self.log_q.put((msg, level))          # 어느 스레드에서 불러도 안전

# _refresh() 안에서
while not self.log_q.empty():
    self.terminal.append_log(*self.log_q.get_nowait())
```

### 3.2 [M2] 지도를 초당 10회 재중심시킨다

```python
# main_controller.py:346-351 (_refresh가 100ms마다 호출)
if MAP_AVAILABLE and lat and lon:
    self.map_widget.set_position(lat, lon)
```

**GPS는 1~10 Hz로 갱신되는데 지도는 100 ms마다 무조건 재중심된다.**
`tkintermapview`가 타일을 계속 다시 요청해 지도가 떨리고 네트워크를 낭비한다.

→ **좌표가 실제로 바뀐 경우에만**, 그리고 **1초에 1회 이하로** 제한할 것.

### 3.3 [M3] 설정이 4개 파일에 흩어져 있다

| 값 | 위치 |
| :--- | :--- |
| `COM3`, `COM10`, 3000000 bps | `lidar_sensor.py:21-23` |
| `COM5`, 115200 | `gps_sensor.py:17-18` |
| `COM6`, 115200 | `imu_sensor.py:21-22` |
| 임계값 7개 | `imu_sensor.py:24-32` |
| `DIST_DANGER/WARN` | `lidar_sensor.py:25-26` |
| Webhook URL | `main_controller.py:23` |

**8인 팀에서 각자 COM 번호가 다르다.** 지금 구조면 전원이 소스를 고치게 되고,
그 상태로 파일을 주고받으면 **충돌과 되돌림이 반복된다.**

→ **`config.py` 또는 `config.yaml` 하나로 모으고, 개인 설정은 `config.local.py`로 분리**하라.
→ 목표 플랫폼은 Windows COM이 아니라 Pi/Jetson의 `/dev/tty*`다. 지금 하드코딩된 COM 번호는
  **이식 시점에 전부 바뀐다.** 계획서 리뷰에서 말한 **udev 고정 심볼릭 링크**(`/dev/lidar_left` 등)를
  전제로 설정을 짜 두면 이식이 한 번에 끝난다.

### 3.4 [M4] IMU 장착 각도 보정이 없다

`roll`/`pitch`를 센서 원값 그대로 쓴다. **IMU를 보행기에 15° 기울여 달면 평상시 pitch가 15°다.**
§1.5에서 추가하기로 한 기울기 조건이 이 오프셋만큼 어긋난다.

→ 기동 시 **"수평 정지 상태 기준값 캡처"** 또는 설정파일의 `MOUNT_ROLL_OFFSET`/`MOUNT_PITCH_OFFSET`으로
  보정하고, **기울기는 항상 오프셋 제거 후 값으로 판단**할 것.

### 3.5 [M5] 스냅샷이 원자적이지 않다

```python
# 필드마다 락을 잡고 놓는다 (imu_sensor.py:160-164)
update("roll", roll); update("pitch", pitch); update("accel_mag", accel_mag) ...
```

`get_snapshot()`이 그 사이에 끼면 **샘플 N의 roll과 샘플 N+1의 accel이 섞인 스냅샷**을 얻는다.
평소엔 무해하지만 **사고 순간의 값을 보고서에 기록하는 시스템**에서는 곤란하다.
100 Hz에서 락을 초당 500회 잡는 비효율도 있다.

→ 한 번에 묶어서 갱신:
```python
def update_many(**kv):
    with _lock: imu_data.update(kv)
```

### 3.6 [M6] `find_gps_port()`의 윈도우 폴백이 위험하다

```python
# sensors/gps_sensor.py:78-80
if system == "Windows":
    for p in ports: return p.device      # 아무 COM 포트나 첫 번째를 반환
```

**LiDAR(COM3)나 IMU(COM6)를 GPS로 열어버릴 수 있다.** 그러면 그 센서의 포트를 빼앗기고
NMEA 파서는 쓰레기를 읽는다. 지금은 `GPS_PORT="COM5"`가 하드코딩돼 이 경로를 타지 않지만,
`None`으로 바꾸는 순간 터진다.

→ **VID:PID로 매칭**하라. u-blox는 VID `0x1546`이다. 설명 문자열 매칭은 보조 수단으로만.

### 3.7 [M7] 정작 부저가 없다 — 경고가 사용자에게 도달하지 않는다

현재 구현된 경고는 **① 화면 전체 깜박임(노트북 GUI) ② Jetson GPIO LED 4개**뿐이다.

- 계획서 §2.2 BOM의 **부저/스피커가 코드에 전혀 없다.**
- **보행기를 미는 고령자는 1280×720 노트북 화면을 보고 있지 않다.**
- 계획서 리뷰 §8에서 지적한 대로 **노인성 난청은 고주파부터 손실**되므로,
  부저를 붙일 때 **1~3 kHz 대역 + 손잡이 진동 병행**으로 설계해야 한다.

→ 현 GUI는 **개발자용 모니터링 도구**로 규정하고,
  **실제 사용자 경고 경로(부저/진동)를 별도로 구현**해야 한다. 이 둘은 다른 물건이다.

### 3.8 [M8] 창 크기가 고정되어 있다

```python
# main_controller.py:396-397
self.root.geometry("1280x720")
self.root.resizable(False, False)
```

Pi에 흔히 쓰는 7인치 터치스크린은 **800×480 또는 1024×600**이다. **창이 화면에 안 들어가는데
크기 조절도 막혀 있다.** 최종 배치 대상 해상도를 먼저 정하고 레이아웃을 반응형으로 짜라.

### 3.9 [M9] 종료 처리가 없다

`stop()` 함수가 세 모듈에 다 있는데 **아무도 호출하지 않는다.** 창을 닫아도 시리얼 포트가
정상적으로 닫히지 않고, `GPIO.cleanup()`도 실행되지 않는다.
Windows에서는 다음 실행 시 **포트 사용 중 오류**로 이어진다.

```python
def _on_close():
    for m in (lidar, imu, gps):
        if m and hasattr(m, "stop"): m.stop()
    root.after(300, root.destroy)      # 스레드 정리 여유
root.protocol("WM_DELETE_WINDOW", _on_close)
```

### 3.10 [M10] 프로젝트 위생

- **git 저장소가 아니다.** 8인 협업에서 이건 그 자체로 최대 리스크다. **오늘 `git init` 하라.**
- `sensors/__pycache__/`에 **`lidar_left.cpython-314.pyc`, `lidar_right.cpython-314.pyc`** 가 남아 있다.
  지금은 없는 모듈이다 — 리팩터링 잔재. `.gitignore`에 `__pycache__/`, `*.pyc`, `config.local.py`, `.env` 추가.
- **`requirements.txt`가 없다.** `pyserial`, `requests`, `tkintermapview` 버전 고정 필요.
- **Python 3.14**로 개발 중인데, Pi/Jetson의 기본 파이썬은 3.10~3.12다.
  `tkintermapview`·`pyserial` 동작 확인 필요. **개발 PC와 타깃의 파이썬 버전을 먼저 맞춰라.**
- `_worker`, `run_integrity_check`의 `except Exception as e`에서 **`e`를 쓰지 않는다.**
  실패 원인(권한 없음 / 포트 없음 / 사용 중)이 로그에 안 남아 디버깅이 어렵다. 메시지에 포함할 것.
- `_validate_checksum`(`gps_sensor.py:90`)은 `*`가 없으면 **`True`를 반환**한다.
  검증 실패는 `False`가 맞다.

---

## 4. 프레임워크 결정 — ROS 2를 미루고 순수 Python으로 간다

### 4.0 결정 사항 (2026-08-20)

**1학기는 ROS 2 없이 순수 Python으로 진행하고, 실기(Jetson + Pi) 이행도 순수 Python으로 한다.**
ROS 2 도입은 P0~P2가 안정화된 이후로 미룬다. **단, §4.3의 5가지 규칙을 지키는 조건으로.**

| 항목 | 계획서 | 실제 코드 | 결정 |
| :--- | :--- | :--- | :--- |
| 프레임워크 | ROS 2 Humble + CycloneDDS | 순수 Python + tkinter | **순수 Python 유지** |
| 보드 구성 | Jetson + Pi 이중 보드 | 단일 프로세스, 단일 PC | **3개 프로세스로 분리** (§4.4) |
| 보드 간 통신 | DDS 토픽 + QoS | 없음 | **UDP JSON** (§4.5) |
| 프로세스 감시 | launch `respawn` | 없음 | **systemd `Restart=always`** (§4.6) |
| 데이터 기록 | rosbag | 없음 | **CSV 로거 + 플롯 스크립트** (§4.7) |
| 경고 출력 | RPi 5 GPIO 부저/LED | Jetson LED + 화면 깜박임 | Pi 부저로 이전 (§3.7) |
| 포트 | `/dev/tty*` | `COM3/5/6/10` | udev 고정 링크 (§3.3) |

**근거 세 가지:**

1. **성능은 쟁점이 아니다.** 부하가 걸리는 곳은 LiDAR 점군 처리 한 곳뿐이고(초당 20~30만 점 규모),
   이는 `numpy` 벡터 연산으로 **1% 미만**으로 떨어진다. 즉 문제는 "파이썬이라서"가 아니라
   **"루프를 파이썬으로 써서"** 생기며, 해결책은 ROS 2가 아니라 numpy다. (§2.3의 수정과 동일한 작업)
   지연시간도 마찬가지다. 현재 최악 35초 중 20초가 FSM 대기시간이므로,
   **ROS 2로 바꿔도 이 20초는 그대로**고 DDS 오버헤드만 추가된다.
   배터리 구동 관점에서는 ROS 2 데몬이 먹는 전력이 오히려 손해다.

2. **안정성 문제의 원인이 ROS 2 부재가 아니다.** §1.2(재연결 없음), §1.3(무응답 감지 없음)은
   ROS 2를 써도 드라이버 노드를 직접 짜면 똑같이 발생한다.
   ROS 2가 주는 실질적 이득은 **프로세스 격리** 하나인데, 이것은 systemd로 동일하게 얻는다.

3. **안전 필수 경로가 보드를 넘지 않는다.** IMU → 판정 → GPS → 웹훅은 **전부 Pi 안에서 끝난다.**
   두 보드 사이를 오가는 데이터는 **좌우 최근접 거리 float 2개, 15~20 Hz**가 전부다.
   여기에 DDS는 명백한 과잉이다.

**포기하는 것과 대체 수단:**

| 잃는 것 | 대체 | 손실 크기 |
| :--- | :--- | :--- |
| `rosbag` | CSV 로거 + matplotlib (§4.7) | 작음 |
| `rviz2` 점군 시각화 | PLY 덤프 → PC의 CloudCompare/Open3D | 중간 (캘리브레이션 시 아쉬움) |
| 벤더 ROS 2 드라이버 | **소스에서 파싱 로직만 추출** | 작음 — 오히려 §1.4 해결에 직접 도움 |

> **§1.4와의 연결:** 현재 LiDAR 프레임 파싱이 데이터시트 확인 없이 추측으로 짜여 있다.
> **Cygbot 공식 ROS 2 드라이버 소스에는 정확한 payload 오프셋과 비트 폭이 들어 있다.**
> ROS 2를 쓰지 않더라도 **그 소스를 읽어서 파싱 규칙만 가져오는 것**이 가장 빠른 해결책이다.
> RTK NTRIP 클라이언트(VRS는 자신의 GGA를 서버로 되돌려 보내야 해서 까다롭다)도 마찬가지다.

> **비기술적 변수:** 학과 평가 기준이나 지도교수가 ROS 2를 요구한다면 위 판단과 무관하게 결론이 바뀐다.
> 그 경우 **Jetson만 ROS 2(벤더 드라이버 활용), Pi는 순수 Python**으로 두고 §4.5의 UDP로 잇는
> 하이브리드가 현실적이다. 인터페이스가 UDP이므로 양쪽이 섞여도 문제없다.

### 4.1 나중에 ROS 2로 옮기는 난이도

**결론: 센서 계층은 기계적이고, 판정 계층은 지금 어디에 두느냐로 갈린다.**

| 구성 요소 | 이행 방식 | 난이도 |
| :--- | :--- | :--- |
| `sensors/*.py`의 프로토콜 파싱 | **그대로 재사용** | 없음 |
| `get_snapshot()` 모듈 | 노드 + 타이머 + 퍼블리셔로 감싸기 | 낮음 (기계적) |
| UDP 패킷 스키마 (§4.5) | `.msg` 정의로 1:1 대응 | 낮음 |
| systemd 유닛 (§4.6) | launch 파일 `respawn=true` | 낮음 |
| 낙상 판정 FSM — **독립 모듈일 때** | 노드 하나로 이동 | 낮음 |
| 낙상 판정 로직 — **GUI 안에 있을 때** | **전면 재작성** | **높음 ← 지금 여기 해당** |
| tkinter GUI | 유지하거나 Foxglove로 교체 | 선택 |
| TF 트리 / 외부 캘리브레이션 | 신규 작업 | ROS 2 여부와 무관 |

**현재 가장 큰 위험 요소:** 사고 판정 로직이 두 곳에 쪼개져 있다.
FSM은 `imu_sensor.py`에 있는데, `_force_accident` / `_alert_sent` / 복귀 판단은
**GUI 클래스의 `_refresh()` 안**(`main_controller.py:694-719`)에 있고,
웹훅 전송도 `SensorManagerGUI`의 메서드다.
**ROS 2에는 GUI가 없으므로, 이 부분은 이행 시 통째로 다시 짜야 한다.**

역으로 말하면 — **판정 로직을 GUI 밖으로 빼내는 작업(§4.3-①)은 ROS 2 이행 비용의 대부분을 미리 없앤다.**
그리고 이 작업은 ROS 2와 무관하게 §1.1과 §4.4를 위해 어차피 해야 한다.

**공수 추정:** §4.3의 규칙을 지킨 경우 **1~2인 × 1~2주**. 지키지 않으면 **3~4주 + 부분 재작성.**
차이는 이행 작업 자체가 아니라 **앞으로 2주간의 설계 결정**에서 결정된다.

**핵심 통찰:** §4.5에서 UDP 프로토콜을 정의하는 행위 = **노드 경계와 메시지 스키마를 확정하는 행위**다.
이는 ROS 2 이행 작업의 90%에 해당한다. **UDP 단계는 낭비가 아니라 예행연습이다.**

```python
# 이행 예시: imu_sensor 모듈은 손대지 않고 노드로 감싼다
class ImuNode(Node):
    def __init__(self):
        super().__init__('imu_node')
        imu_sensor.start()
        self.pub = self.create_publisher(Imu, '/imu/data', qos)
        self.create_timer(0.01, self.tick)     # 100 Hz

    def tick(self):
        s = imu_sensor.get_snapshot()
        if not s["valid"]: return
        msg = Imu()
        msg.header.stamp    = self.get_clock().now().to_msg()
        msg.header.frame_id = "imu_link"
        # 주의: ROS 표준 linear_acceleration 단위는 m/s² → g에서 변환
        msg.linear_acceleration.x = s["ax"] * 9.80665
        self.pub.publish(msg)
```

### 4.2 이행 순서

**§1의 Critical 6건을 순수 Python 상태에서 먼저 잡고, 그다음에 ROS 2를 검토한다.**
동작하지 않는 로직을 ROS 2로 옮기면 **버그가 프레임워크 문제인지 로직 문제인지 구분할 수 없게 된다.**
이 구분이 안 되는 상태로 디버깅하면 몇 주가 사라진다.

> 이행을 실제로 착수할 때는 계획서 리뷰 §2.1(**Pi 5의 이더넷은 `eth0`이 아니라 `end0`**)과
> §7-4(**ROS 2 배포판 통일 — Pi 5는 Ubuntu 22.04 미지원**)를 먼저 확인할 것.

### 4.3 이행 비용을 낮게 유지하는 5가지 규칙

이 규칙들은 ROS 2를 위한 것이 아니라 **지금 당장의 안정성을 위한 것이기도 하다.**
지키면 이행이 껍데기 교체로 끝나고, 어기면 재작성이 된다.

**① GUI는 읽기 전용. 판정 로직 0줄.**
GUI는 `get_snapshot()`으로 읽어서 그리기만 한다. 상태를 바꾸거나 신고를 보내지 않는다.
현재 `_force_accident`, `_alert_sent`, `_waiting_recovery`, `_send_emergency_report`는
**전부 GUI 밖의 `tipover_monitor` 모듈로 이동해야 한다.**
GUI를 껐다 켜도 낙상 감지가 아무 영향 없이 계속되면 이 규칙을 지킨 것이다.

**② 모듈 하나 = 미래의 노드 하나.**
모듈 간에는 직접 함수 호출 대신 **정의된 스키마의 데이터**만 주고받는다.
`imu_sensor`가 `gps_sensor`를 import하기 시작하면 노드로 쪼갤 수 없게 된다.

**③ 메시지 스키마를 지금 코드로 못박는다.**
dict를 즉석에서 만들지 말고 `dataclass`로 선언한다. 이것이 나중에 `.msg`가 된다.

```python
# schema.py — 이 파일이 미래의 msg/ 디렉터리다
from dataclasses import dataclass

@dataclass
class ImuSample:
    t_capture: float      # 수집 시각 (time.time)
    roll: float           # deg, 장착각 보정 후
    pitch: float          # deg, 장착각 보정 후
    accel_g: float        # g (ROS 이행 시 × 9.80665 → m/s²)
    gyro_dps: float       # deg/s
    valid: bool

@dataclass
class ObstacleReport:
    t_capture: float
    left_m:  float | None   # None = 감지 없음
    right_m: float | None
    left_ok:  bool          # False = 센서 이상
    right_ok: bool
```

**④ 단위·좌표계·타임스탬프를 지금 고정한다.**
- 단위: **거리 m, 각도 deg, 가속도 g** (변수명에 단위를 붙인다: `accel_g`, `left_m`, `roll_deg`)
- 좌표계: `base_link` 기준 x-전방 / y-좌 / z-상 (REP-103)
- **모든 샘플에 수집 시각을 붙인다.** ROS 메시지의 `header.stamp`가 그대로 여기서 온다.
  이건 §1.3(무응답 감지)에서 어차피 필요하다.

**⑤ 네트워크·블로킹 I/O는 전용 스레드/프로세스에.**
ROS 2 콜백은 블로킹하면 안 된다. 지금부터 같은 규율을 지키면 이행 시 손댈 게 없다.
`_send_emergency_report`의 스레드 분리는 이미 이 규칙을 지키고 있다 — 유지할 것.

### 4.4 프로세스 분리 설계

**현재 가장 심각한 구조 문제: GUI와 낙상 감지가 같은 프로세스에 있다.**
tkinter mainloop가 지도 타일을 받다 멈추거나 위젯에서 예외 하나만 나도 **낙상 감지가 같이 죽는다.**
계획서의 "Jetson이 죽어도 Pi 단독으로 낙상 알림은 살아 있어야 한다"는 요구사항이,
**지금은 같은 보드 안에서조차 지켜지지 않고 있다.**

| 보드 | 프로세스 | 담당 | 죽으면? |
| :--- | :--- | :--- | :--- |
| **Pi 5** | `safety_monitor.py` | IMU + GPS + 판정 FSM + 웹훅 + **부저** | **치명 — systemd가 2초 내 재시작** |
| **Pi 5** | `ui.py` | tkinter 대시보드 (읽기 전용) | **무영향** — 안전 기능 계속 동작 |
| **Jetson** | `lidar_node.py` | LiDAR 2대 + 점군 처리 + UDP 송신 | 장애물 경고만 중단, 낙상 감지는 생존 |

프로세스 간 데이터는 **UDP(보드 간)** 와 **로컬 UDP 또는 파일/소켓(같은 보드 내 GUI)** 으로 나른다.
GUI에는 같은 UDP 스트림을 `127.0.0.1`로 한 벌 더 보내면 된다 — 코드가 하나로 통일된다.

### 4.5 보드 간 UDP 프로토콜

**설계 원칙 3가지**

1. **비신뢰 UDP가 정답이다.** 장애물 거리는 "최신값만 의미 있음" 성격이므로
   재전송이 오히려 해롭다. (계획서 §3.3에서 이 토픽을 `BEST_EFFORT`로 정한 것과 같은 판단이다.)
2. **데이터가 없어도 계속 보낸다.** 패킷 자체가 하트비트 역할을 한다.
   "값이 없음"과 "링크가 끊김"을 수신 측이 구분할 수 있어야 한다.
3. **신선도 판정은 수신 측 도착 시각으로 한다.** 송신 측 타임스탬프는 진단용으로만 쓴다.
   → **두 보드의 시계 동기화에 의존하지 않는다.** (chrony가 어긋나도 오동작하지 않는다)

```python
# ═══════════════════ Jetson: lidar_tx.py ═══════════════════
import json, socket, time
from sensors import lidar_sensor as lidar

PI_ADDR   = ("192.168.10.2", 9100)
UI_ADDR   = ("127.0.0.1",    9100)   # 같은 보드에 GUI가 있으면 여기에도
SEND_HZ   = 20
SCHEMA_V  = 1

def main():
    sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
    lidar.start()
    seq, period = 0, 1.0 / SEND_HZ
    next_t = time.monotonic()

    while True:
        snap = lidar.get_snapshot()
        seq += 1
        pkt = {
            "v":   SCHEMA_V,
            "seq": seq,
            "t":   time.time(),                 # 송신 시각 — 진단용
            "left":  {"d": snap["left"]["min_dist"],
                      "ok": snap["left"]["status"] == "정상"},
            "right": {"d": snap["right"]["min_dist"],
                      "ok": snap["right"]["status"] == "정상"},
        }
        blob = json.dumps(pkt).encode()
        # 데이터가 없어도 매 주기 전송 = 하트비트
        for addr in (PI_ADDR, UI_ADDR):
            try: sock.sendto(blob, addr)
            except OSError: pass                # 네트워크 일시 장애로 죽지 않는다

        next_t += period
        time.sleep(max(0.0, next_t - time.monotonic()))

if __name__ == "__main__":
    main()
```

```python
# ═══════════════════ Pi: lidar_rx.py ═══════════════════
import json, socket, threading, time

LISTEN     = ("0.0.0.0", 9100)
STALE_SEC  = 0.5        # 이 시간 넘게 못 받으면 "링크 끊김"
SCHEMA_V   = 1

_lock  = threading.Lock()
_state = {"left": None, "right": None, "left_ok": False, "right_ok": False,
          "seq": 0, "arrived": 0.0, "latency": None}

def get_snapshot() -> dict:
    """링크가 끊겼으면 거리값을 절대 내보내지 않는다 (§1.3 무응답 감지)."""
    with _lock:
        s = dict(_state)
    s["link_ok"] = bool(s["arrived"]) and (time.monotonic() - s["arrived"]) < STALE_SEC
    if not s["link_ok"]:
        s["left"] = s["right"] = None
        s["left_ok"] = s["right_ok"] = False
    return s

def _rx_loop():
    sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
    sock.bind(LISTEN)
    sock.settimeout(0.2)
    while True:
        try:
            data, _ = sock.recvfrom(2048)
            pkt = json.loads(data)
            if pkt.get("v") != SCHEMA_V:
                continue
            with _lock:
                prev = _state["seq"]
                # 순서 역전 패킷은 폐기. 단 seq가 크게 뒤로 가면
                # Jetson 재시작이므로 새 세션으로 받아들인다.
                if pkt["seq"] <= prev and (prev - pkt["seq"]) < 1000:
                    continue
                _state.update(
                    seq      = pkt["seq"],
                    left     = pkt["left"]["d"]  if pkt["left"]["ok"]  else None,
                    right    = pkt["right"]["d"] if pkt["right"]["ok"] else None,
                    left_ok  = pkt["left"]["ok"],
                    right_ok = pkt["right"]["ok"],
                    arrived  = time.monotonic(),
                    latency  = time.time() - pkt["t"],   # 시계 동기화 진단용
                )
        except socket.timeout:
            continue
        except Exception:
            continue          # 깨진 패킷 하나로 수신 스레드가 죽지 않는다

def start():
    threading.Thread(target=_rx_loop, daemon=True, name="LiDAR-RX").start()
```

**사용 측 (`safety_monitor.py` / `ui.py`):**

```python
s = lidar_rx.get_snapshot()
if not s["link_ok"]:
    buzzer.sensor_fault()          # "장애물 경고 사용 불가" 고지
else:
    warn_by_distance(s["left"], s["right"])
# ★ 어느 경우든 낙상 감지는 계속 동작한다 — 요구사항 자동 충족
```

> **ROS 2 이행 시:** 위 `pkt` 딕셔너리가 그대로 커스텀 `.msg`가 되고,
> `lidar_tx.py`는 퍼블리셔로, `lidar_rx.py`는 서브스크라이버 콜백으로 바뀐다.
> **`STALE_SEC` 로직은 QoS Deadline으로 대체 가능하지만, 그냥 두어도 무방하다.**

### 4.6 systemd 유닛 (프로세스 감시)

> **중요:** 계획서 리뷰 §2.1에서 지적했듯 **systemd 서비스는 `.bashrc`를 읽지 않는다.**
> 환경변수는 반드시 `EnvironmentFile`로 넘겨야 한다. §2.1의 웹훅 URL도 여기로 들어간다.

```ini
# /etc/systemd/system/walker-safety.service   ← 안전 필수. 절대 죽으면 안 됨
[Unit]
Description=Walker fall detection (safety critical)
After=network-online.target

[Service]
Type=simple
User=walker
WorkingDirectory=/opt/walker
EnvironmentFile=/etc/walker/secrets.env
ExecStart=/opt/walker/venv/bin/python -u safety_monitor.py
Restart=always
RestartSec=2
StandardOutput=append:/var/log/walker/fall.log
StandardError=append:/var/log/walker/fall.log

[Install]
WantedBy=multi-user.target
```

```ini
# /etc/systemd/system/walker-ui.service     ← 죽어도 안전 기능에 무영향
[Unit]
Description=Walker dashboard (non-critical)
After=walker-safety.service
# ★ Requires= 를 쓰지 말 것. UI 실패가 fall 서비스를 끌어내리면 안 된다.

[Service]
Type=simple
User=walker
WorkingDirectory=/opt/walker
Environment=DISPLAY=:0
ExecStart=/opt/walker/venv/bin/python -u ui.py
Restart=on-failure
RestartSec=5

[Install]
WantedBy=graphical.target
```

```ini
# Jetson: /etc/systemd/system/walker-lidar.service
[Unit]
Description=Walker LiDAR node
After=network-online.target

[Service]
Type=simple
User=walker
WorkingDirectory=/opt/walker
ExecStart=/opt/walker/venv/bin/python -u lidar_tx.py
Restart=always
RestartSec=2

[Install]
WantedBy=multi-user.target
```

```bash
sudo install -d -m 700 /etc/walker && sudo chmod 600 /etc/walker/secrets.env
sudo systemctl daemon-reload
sudo systemctl enable --now walker-safety walker-ui
journalctl -u walker-safety -f          # 실시간 로그 확인
```

`/etc/walker/secrets.env` (권한 600, **git에 절대 넣지 말 것**):

```
WALKER_WEBHOOK_URL=https://discord.com/api/webhooks/...
NTRIP_USER=...
NTRIP_PASS=...
```

> **`-u` 플래그를 반드시 붙일 것.** 파이썬 stdout 버퍼링 때문에
> 크래시 직전 로그가 통째로 사라지는 일이 흔하다. 원인 분석이 불가능해진다.

> **ROS 2 이행 시:** 위 3개 유닛이 launch 파일의 `Node(..., respawn=True, respawn_delay=2.0)`
> 3줄로 대체된다. **프로세스 경계를 지금 정해두면 그때 그대로 옮겨진다.**

### 4.7 rosbag 대체 — CSV 로거

계획서 §6.2의 **낙상 임계값 실험 데이터 수집**에 필요하다.
지금 코드에는 로깅이 전혀 없어서, 실험을 해도 **근거 자료가 남지 않는다.**

```python
# logger.py — safety_monitor에서 항상 켜둔다
import csv, time, pathlib

class SampleLogger:
    def __init__(self, tag: str):
        d = pathlib.Path("logs"); d.mkdir(exist_ok=True)
        name = f"{tag}_{time.strftime('%Y%m%d_%H%M%S')}.csv"
        self.f = open(d / name, "w", newline="", encoding="utf-8")
        self.w = csv.writer(self.f)
        self.w.writerow(["t", "roll_deg", "pitch_deg", "accel_g", "gyro_dps",
                         "state", "left_m", "right_m", "fix_q"])
        self.n = 0

    def write(self, *row):
        self.w.writerow(row)
        self.n += 1
        if self.n % 200 == 0:      # 2초마다 flush — 전원이 끊겨도 데이터가 남는다
            self.f.flush()
```

**실험 프로토콜:** 시나리오마다 `tag`를 다르게 준다.
`normal_walk`, `curb`, `sudden_stop`, `place_down`, `car_load`, `tipover_front`, `tipover_side` …
그러면 파일명만으로 양성/음성 샘플이 분류되고, 아래 스크립트로 임계값을 결정할 수 있다.

```python
# analyze.py — 임계값 결정용
import glob, pandas as pd, matplotlib.pyplot as plt

for path in sorted(glob.glob("logs/*.csv")):
    df = pd.read_csv(path)
    label = path.split("/")[-1].split("_")[0]
    print(f"{label:14s} accel_g 최대={df.accel_g.max():5.2f}  "
          f"기울기 최대={df[['roll_deg','pitch_deg']].abs().max().max():5.1f}°")
    plt.plot(df.t - df.t.iloc[0], df.accel_g, label=label, alpha=0.7)

plt.axhline(2.8, ls="--", c="r", label="현재 임계값 2.8g")
plt.xlabel("시간 (s)"); plt.ylabel("가속도 벡터합 (g)"); plt.legend(); plt.show()
```

이 그래프 한 장이 **"낙상(양성)과 방지턱·내려놓기(음성)의 가속도 분포가 겹치지 않는 지점"** 을
보여준다. 그것이 `IMPACT_THRESHOLD`의 근거가 되고, **보고서의 핵심 그림**이 된다.
지금처럼 `2.8`이라는 숫자를 근거 없이 쓰면 심사에서 반드시 질문을 받는다.

> **주의:** 임계값 실험 전에 §1.6의 **가속도계 측정범위(FSR)** 확인이 선행되어야 한다.
> FSR이 ±2 g면 그래프 자체가 2 g에서 잘려 나온다.

---

## 5. 우선순위 — 이 순서대로 고쳐라

> **2026-08-21 적용 완료.** 아래 1~18번과 §4의 구조 개편(프로세스 분리, UDP, systemd)이
> `capstone_code/`에 반영되었다. 이전 단일 프로세스 버전은 `capstone_code/legacy/`에 보존.
> **검증: 자동 테스트 31종 전부 통과** (`python tests/run_all.py`, 1초 소요),
> 시뮬레이터로 8개 시나리오 전 구간 동작 확인, 하드웨어 없는 상태에서 재연결 확인.

### 이번 주 (동작 복구) — ✅ 완료

1. ✅ **[C1]** 상태 문자열을 공유 상수 모듈로 통일 → `states.py`
2. ✅ **[C5]** 낙상 확정 조건에 기울기(60°) 관문 추가 → `tipover_monitor.py`
3. ✅ **[H1]** Webhook을 환경변수로 + 실제로 동작하는 검증 함수 → `notifier.py`
4. ✅ **[C6]** `len(parsed) != 9` + 정지 시 1 g 자체 진단 → `sensors/imu_sensor.py`
5. ✅ **[C3]** `t_capture` 기록 + 신선도 강등 (`snapshot()`이 낡은 값을 내보내지 않음)

### 다음 주 (신뢰성) — ✅ 완료 (C4 일부 제외)

6. ✅ **[C2]** 세 모듈 재연결 루프 (지수 백오프 1→2→4→…→30초)
7. ⚠ **[C4]** 롤링 버퍼 재동기화 + 블록 읽기 적용. **payload 포맷은 여전히 미검증** —
   실기에서는 Jetson의 벤더 ROS 2 드라이버가 대체하므로 우선순위가 낮아졌으나,
   PC 개발용 `lidar_serial.py`를 쓰려면 줄자 실측이 필요하다
8. ✅ **[H3]** 퍼센타일 + 최소거리 하한 + 연속 프레임 확인
9. ✅ **[H7]** 신고 메시지에 측위 품질 / 위성 수 / HDOP / 좌표 신선도 포함
10. ✅ **[H2]** 예비 알림 → 확정/해제 알림 2단 구조, 지도는 후속 첨부

### 후순위 과제 — 2026-08-21 일괄 처리 완료

계획서 리뷰와 §5에서 "하드웨어가 필요하다"며 미뤄두었던 항목 중,
**코드로 미리 만들어 둘 수 있는 것은 전부 구현했다.** 하드웨어가 도착하면
배선하고 설정값만 맞추면 되는 상태다.

| 항목 | 구현 | 남은 일 |
| :--- | :--- | :--- |
| **[M7] 부저 · LED · 손잡이 진동** | `alerting.py` — 7단계 우선순위, 고령자 가청 대역(1~3 kHz), 거리 3단계 패턴, 히스테리시스, 경보 피로 억제, 정지 중 경고 중단, 센서 이상 주기 고지 | GPIO 결선 |
| **취소 · SOS 물리 버튼** | `buttons.py` — 디바운스, SOS는 2초 길게 누름만 인정(주머니 눌림 방지), 개발용 키보드 백엔드 | 버튼 2개 조달 |
| **SOS 경로** | 판정 FSM을 전부 건너뛰고 즉시 신고. 낙상이 아닌 응급(현기증·흉통)은 자동 감지로 잡을 수 없다 | — |
| **[계획서 §6.2] NTRIP / RTK** | `ntrip_client.py` — NTRIP v1/v2, VRS용 GGA 되올림, RTCM3 → F9P 전달, 지수 백오프 재접속, 소스테이블 조회 | **LTE 회선** |
| **[H5] 배터리 게이지** | `battery.py` — INA219 / MAX17043 / sysfs 백엔드, 리튬 전압-잔량 곡선, 20/10/5% 경고, **저전압 안전 종료** | I2C 모듈 조달 |
| **[C4] LiDAR 포맷 판별** | `tools/lidar_probe.py` — 오프셋 0~16 × 패킹 4종 전수 탐색, 줄자 실측값으로 정답 확정, 체크섬 알고리즘 판별. 파서는 config로 전환 가능하게 변경 | 10분 실측 |
| **[P3] 단차(턱) 감지** | `jetson/lidar_bridge_node.py` — 지면 평면 추정 후 편차로 drop/rise 판별. UDP·UI·경보까지 배선 완료 | 3D 모드 + 틸트 장착 |
| **[§4.7] 임계값 근거 도구** | `tools/replay.py` — 기록 로그를 **실제 판정 코드**에 가상 시계로 재생. `--sweep`으로 임계값 전수 탐색, 완전 분리 구간과 중앙값 산출 | 실측 데이터 |
| **하드웨어 없는 개발** | `sensors/sim.py` — 8개 시나리오 시뮬레이터. 8인 팀에 센서는 한 세트뿐인데 나머지가 대기하는 상황을 없앤다 | — |
| **자동 테스트** | `tests/` — 31종. pytest 없이도 실행 가능, 1초 소요 | — |

### 그 과정에서 추가로 발견해 고친 결함

- **CSV가 10 Hz로 기록되고 있었다.** 상태 표시 주기로 로그를 남기고 있어서,
  **충격 피크를 통째로 놓치는** 데이터가 쌓일 뻔했다(강체 충돌은 수십 ms에 끝난다).
  → 주 루프를 센서 원속도(100 Hz)로 올리고 상태 송신만 10 Hz로 분리.
  `tools/replay.py`를 만들어 돌려보고 나서야 드러난 문제다.
- **미전송 알림이 프로세스와 함께 사라졌다.** LTE가 끊긴 순간에 사고가 나고
  프로세스가 죽으면 그 알림은 영영 통보되지 않는다.
  → `notifier.py`에 디스크 스풀(fsync) 추가. 재기동 시 이어서 전송하며,
  메시지에 **"지연 발송"** 과 사고 발생 시각을 표시한다.
- **`tools/`의 표 출력도 cp949에서 죽었다.** → `console.py`로 통일.

### 구현 중 추가로 발견해 고친 것

- **사용자 취소 유실**: 명령이 상태 전이와 같은 틱에 도착하면 그대로 버려졌다.
  → 명령에 3초 유효기간을 두고, 판정 중인 어느 단계에서든 "아니오"를 받아들이도록 수정
- **반복 통보**: 해제 후에도 보행기가 기울어 있으면 작은 충격 하나로 즉시 재확정되었다.
  → `REARM_COOLDOWN`(5초) 도입
- **윈도우 콘솔 크래시**: 로그의 `✅`/`⚠` 문자가 cp949에서 `UnicodeEncodeError`를 일으켜
  **안전 필수 프로세스가 로그 한 줄 때문에 죽을 수 있었다.** → stdout UTF-8 강제 + 예외 격리

### 그 다음 (구조)

> 이 단계의 목표는 §4.3의 5가지 규칙을 코드에 반영하는 것이다.
> **특히 11번(판정 로직을 GUI 밖으로)이 ROS 2 이행 비용의 대부분을 미리 없앤다.**

11. **[§4.3-①][§4.4]** **판정 로직을 GUI에서 분리** → `tipover_monitor.py`,
    프로세스 3개로 나누고 systemd 등록 (§4.6) — *현재 GUI가 죽으면 낙상 감지도 죽는다*
12. **[§4.7]** CSV 로거 투입 — *임계값 실험(계획서 §6.2)의 전제. 지금 안 넣으면 실험해도 근거가 안 남는다*
13. **[§4.5]** 실기 이행 시 UDP 프로토콜 적용 (PC 단독 개발 중에는 불필요)
14. **[M3]** `config.py` 통합 + `.gitignore` + `requirements.txt` + **`git init`**
15. **[H6][M4]** 움직임 판정에 시간 간격·이동평균, IMU 장착각 보정
16. **[H5]** 실제 배터리 게이지 연동 (그전까지는 "측정 안 됨" 표시)
17. **[M7]** 부저/진동 경고 경로 구현 — *GUI는 개발자용, 사용자 경고는 별개*
18. **[M1][M2][M9]** 로그 큐 전환, 지도 갱신 스로틀, 종료 처리

---

## 6. 잘 되어 있어서 유지할 것

지적이 길어서 균형을 위해 명시해 둔다.

- **모듈 경계가 깨끗하다.** GUI가 시리얼 프로토콜을 전혀 모른다. 이 설계 덕분에
  §4의 ROS 2 이행이 "껍데기 씌우기"로 끝난다. **이 경계를 앞으로도 깨지 마라.**
- **`try/except ImportError` 로 센서 없이도 GUI가 뜬다.** 8명이 각자 다른 하드웨어를 들고
  개발할 때 이 설계가 큰 도움이 된다.
- **`_lock` 사용 습관**이 처음부터 들어 있다. §3.5의 원자성만 보완하면 된다.
- **FSM으로 즉시 신고를 피하고 사용자 확인 단계를 넣은 것** — 계획서 리뷰에서 요구했던
  취소 경로가 이미 있다. §1.5의 자세 조건만 추가하면 실용 수준이 된다.
- **`run_integrity_check`의 3회 재시도와 단계별 터미널 로그** — 현장 디버깅에 실제로 유용하다.
  §2.9(유효 데이터 수신 확인)까지만 보강하면 완성이다.
- **`_send_emergency_report`에서 lat/lon이 None일 때 "알 수 없음"으로 처리** —
  계획서 코드에 있던 `(0, 0)` 전송 버그가 여기서는 이미 고쳐져 있다.
